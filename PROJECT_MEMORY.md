# 모두의사업친구 — 프로젝트 핵심 메모리 (항상 최신화)

> 대표님 요청: "대화 핵심은 항상 여기에 저장해서 최신화" · 크레딧 절약 원칙

## 사업 정체성
- **대표**: 신주엽 (인천 서구 청라). 직업군인·경영컨설턴트 출신, 정부지원사업 경력 약 3년.
- **실적**: 승인 컨설팅 100여 건, 지원금 100억+ (바우처 포함).
- **사업**: 정부지원사업 AI 통합 매칭 플랫폼.
- **회사가 크는 핵심(대표님 말)**: "그 사람한테 해당되는 정부지원사업/감면제도/정책자금을 얼마나 정확하게, 상품명 맞게, 보증서만 나오는지·재단 거쳐야 하는지·대출까지 한 번에 되는지를 잘 안내해야 CS 줄고 회사가 성공."
- **매출 경로**: 388→603→1,979→목표 3,000만/월. 컨설팅+대행 수수료로 3,000만 사정권. 자문·세미나는 추가매출(조급 금지).

## 기술 스택
- Next.js 14 App Router + TypeScript + TailwindCSS + Supabase Auth + Vercel(자동배포).
- GitHub: `ppfa26/PPFA26`, 항상 **main** 브랜치, 코드 `/home/user/webapp`.
- 도메인: `모두의사업친구.kr` = 퓨니코드 `xn--2e0br4kgyfb0gp4gbrcj9s.kr` (HTTP 200 확인).
- Supabase: URL `https://akjlulfbhoukbhuyaybo.supabase.co`, Callback `.../auth/v1/callback`. "Allow users without an email"=OFF.
- 카카오/구글 로그인 실제 작동 확인됨. 카카오싱크 이름·전화번호 동의항목 검수 중(3~5영업일).

## 절대 규칙
- **매칭 스코어링 로직(matching.ts)은 절대 안 건드림** — 표시 단계 후처리만 추가.
- 리포트는 **팩트 기반**.
- 이해 안 되면 실행 전 대표님께 다시 질문.
- 크레딧 많이 쓰는 작업 지양.

## 배포/작업 명령
- 타입체크: `npx tsc --noEmit -p tsconfig.json`
- 빌드: `npm run build` (300s)
- 푸시: `GHTOK=$(gh auth token) && git -c credential.helper= push "https://x-access-token:${GHTOK}@github.com/ppfa26/PPFA26.git" HEAD:main`
  - 토큰 만료 시("Invalid username or token") → `setup_github_environment` 재호출 후 다시 위 명령.
- 실서버 검증(선택): `pm2 start ecosystem.config.cjs`(next start -p 3000) → `curl localhost:3000` → 끝나면 `pm2 delete all`.
- PDF/스샷: Playwright Chromium (`~/.cache/ms-playwright/chromium-1228`), 모듈 `NODE_PATH=/opt/npm-cache/_npx/e41f203b7505f1fb/node_modules`. 모바일 390×844 / PC 1280×900.

---

# ✍️ SNS 글쓰기 플레이북 — 5채널 동시 홍보글 (다음번 글 쓸 때 이 섹션만 보고 생성)
> 대표님 요청: "글쓰기 좋은 정보성 글 많이 참고, 성공 홍보 사례를 우리 글에 접목." 아래 공식·문구은행·사례교훈을 근거로 매번 뽑는다. **표시/카피만, 서비스 팩트는 왜곡 금지.**

## 0. 참고한 성공 사례 & 핵심 교훈 (근거)
- **삼쩜삼(자비스앤빌런즈) — 1년 740만 가입**: [출처 blog.3o3.co.kr] 배운 것 5가지 →
  1) **'고객의 언어'로 가치 표현**: "종합소득세 환급" 같은 전문용어 ❌ → "꽁돈·눈먼돈·놓친 돈" 같은 체감 단어 ⭕. → 우리는 "정부지원사업"보다 **"안 받으면 사라지는 돈 / 남이 가져가는 돈 / 갚을 필요 없는 돈"**.
  2) **소멸 트리거(FOMO)**: "신청 안 하면 사라짐, 5년 지나면 못 받음" → 즉시 반응. → 우리는 **"공고 시기 놓치면 1년 뒤"**, **"예산 소진되면 마감"**.
  3) **'신청 안 하면 안 주는 눈먼돈' 정보성 게시글**: 저예산으로 343만 도달·9만 공유저장. → **정보성 + 소멸감** 결합이 핵심.
  4) **타깃 세분화 → 언어를 다르게**: 20대엔 20대 언어. → 우리는 채널별(스레드·인스타=도발 / 네이버=신뢰 / 당근=지역 / 카톡=기존고객)로 톤 분리.
  5) **실제 후기·사례가 광고보다 2~10배 효율**: 진정성. → 우리는 **실제 승인 사례(신주엽 대표 100여건·100억+)**를 팩트로 녹임(단, 개인정보/과장 금지, "승인 보장 안 함" 고지 유지).
- **카드뉴스 후킹 공식 [출처 gogumafarm.kr]** — 좋아요 4천~9천 게시물 분석:
  1) 제목에 **내용을 다 말하지 말고 궁금증만** ("서울숲 명소 39곳" ❌ → "서울숲 가야 하는 이유" ⭕).
  2) 같은 정보라도 **표현만 바꾸면 신선** ("감성카페" → "해외st 카페").
  3) **핵심 메시지 함축** ("희소성" → "나만 알고 싶은").
  4) **친구 태그·댓글 유도**로 소통 활성화.

## 1. 검증된 후킹 제목 템플릿 (OO=정부지원/지원금/사장님)
- `OO 안 하면(안 받으면) 손해봄` · `안 받으면 그 돈, 남이 가져갑니다`
- `어차피 알려줘도 100명 중 97명은 안 함` (도발+FOMO, 스레드용)
- `사장님이 놓치는 OO n가지` · `OO, 이것만 보면 됨`
- `OO이 궁금한 사람 지금 눌러봐` · `내가 OO 받는 법 알려줄게`
- `30초면 내 지원금이 보입니다` (혜택 즉시성)
- `이거 몰라서 못 받는 사장님 진짜 많음`

## 2. 글 구조 공식 (원문 스레드 구조 = 우리 표준)
**① 본문(후킹)**: 도발적 첫 문장 → 반전 정보 1줄 → FOMO("안 하면 손해/사라짐") → `아래 👇` 로 답변 유도.
**② 답변1(정보 나열)**: 놓치기 쉬운 항목 3가지 = ①무상 사업화 자금 ②정책자금(낮은금리) ③세금 감면·환급. 각 1~2줄 + 팩트(공고시기 등).
**③ 답변2(CTA)**: "30초 무료 진단 → 모두의사업친구" + **필수 고지** "안내·매칭 서비스이며 승인 보장 안 함".

## 3. 채널별 톤 규칙 (동시 배포 시 미세조정)
- **스레드/인스타**: 도발·반말·FOMO 최대. 이모지 👇⚠️. 짧은 줄바꿈.
- **네이버(블로그/카페)**: 신뢰·정보형 존댓말. 소제목 ▪️. 팩트 강조. SEO 키워드(정부지원금·정책자금·소상공인).
- **당근**: 지역·친근("인천에서 운영하는 대표"). 부담↓("결과만 보고 판단하셔도 돼요").
- **카카오톡 채널소식**: 기존 고객 대상, 따뜻하게. "혹시 이거 확인하셨나요?" 톤.
- **공통**: 링크는 한글 `모두의사업친구.kr`(퓨니코드 노출 금지). 위험/과장 표현 금지. 승인보장 부인 고지 항상.

## 4. 절대 지킬 것
- 서비스 사실(무료진단·매칭·승인 미보장) 왜곡 금지. "무조건 받는다" 류 금지(삼쩜삼도 허위광고 과징금 사례 있음 → 반면교사).
- 실적 언급은 팩트 범위(대표 경력·승인 이력)만, 개인정보/특정 금액 과장 금지.
- 색/디자인은 브랜드 팔레트(옐로→오렌지, 다크). 썸네일 문구는 위 후킹 템플릿에서.

---

# 🔁 재현 플레이북 — 아이템(사업)만 바뀌어도 이 수준 홈페이지 재현
> "다음에 아이템만 바뀌어도 이 정도 홈페이지를 만들 수 있게" — 대표님 요청으로 핵심 노하우 총정리. 새 프로젝트 시 이 섹션만 보고 그대로 재현 가능.

## A. 기술 스택 & 초기 셋업
- **Next.js 14 App Router + TypeScript + TailwindCSS + Supabase Auth + Vercel(자동배포)**. 코드는 `src/app/`.
- GitHub push → Vercel 자동 배포. 항상 **main** 브랜치.
- 실서버 검증은 PM2로 `next start -p 3000`(`ecosystem.config.cjs`).
- 한글 도메인 쓸 땐 링크를 `window.location.origin`(퓨니코드 xn-- 노출) 대신 **한글 URL 하드코딩**.

## B. 브랜드 색상 팔레트 (tailwind.config.ts)
- brand.yellow `#FEE500` / gradFrom `#FFD500` / gradTo `#FF9500` / orange `#FF6F0F`
- brand.dark `#191919` / gray `#6B7280` / green(emerald) `#00C471` / red `#FF3B30`
- 폰트: Pretendard → Noto Sans KR → system-ui. 그라데이션: `brand-grad`(135deg #FFD500→#FF9500). 그림자: `card`/`cardHover`. 반응형 `xs:400px`.
- **색상 사용 규칙(절대)**: 기본 UI/버튼 = **흰색·회색·반투명(white)**. 상태 배지 = **초록/오렌지**. **위험 액션(삭제·계정차단·환불)만 레드 유지**. 브랜드 강조(CTA·핵심포인트)만 옐로/오렌지 그라데이션. 그 외 남발 금지(indigo/sky/rose/amber 등 → blue/orange/red/green으로 통일).

## C. 다크 야경 테마 구조 (globals.css)
- 몰입형 페이지(home/community/admin 등)는 `<body class="theme-dark">`. 밝은 페이지 코드(bg-white/text-brand-dark)를 전역 CSS가 자동으로 **반투명 글래스 + 밝은 글자**로 변환.
- `body { color:#f4f5f7; background-color:#0b1020; overflow-x:hidden; }`
- **배경 고정 레이어(핵심)**: `body::before { content:""; position:fixed; inset:0; z-index:-1; pointer-events:none; background-image: linear-gradient(오버레이) , url(야경); background-size:cover; background-position:center top; }`
  - ⚠️ `body`에 직접 `background-attachment:fixed`를 주면 **iOS 사파리 스크롤 떨림/튐 버그**. 반드시 `body::before` 고정레이어로 분리해야 PC·모바일 모두 100% 고정.
  - `body.theme-dark`는 **배경색 미지정**이어야 ::before가 정상 노출됨.
- **오버레이 어둡기**(야경 위 가독성): `linear-gradient(rgba(8,12,24,A1), rgba(8,12,24,A2))`. 현재값 PC `0.78~0.90`, 모바일(`@media max-width:640px`) `0.82~0.94`. 글씨 안 보이면 ↑, 너무 답답하면 ↓.
- 글래스 컴포넌트: `.hero-glass`(rgba(18,23,36,0.55)+blur18+border rgba(255,255,255,0.14)), `.section-title-glass`(rgba(18,23,36,0.5)+blur14), `.pricing-card-popular`(rgba(20,25,38,0.62)+골드테두리).
- 다크배경이라 테두리는 **`border-white/10`**(반투명 흰). `border-gray-200`은 라이트 잔재지만 다크테마 CSS가 커버하므로 강제 통일 불필요.

## D. 배경 이미지 생성·최적화 워크플로
1. 기존/원하는 색감 참조 → **nano-banana-pro, image-to-image**(`image_urls`에 기존 배경 넣어 색조 유지). 여러 안 생성 후 실제 오버레이+히어로 얹은 스샷 비교로 채택.
2. genspark file URL은 **curl 받으면 59바이트 JSON 인증에러** → 반드시 **DownloadFileWrapper 도구**로 다운로드.
3. ImageMagick 최적화(crop-to-fill):
   - PC: `convert src.jpg -resize 1920x1072^ -gravity center -extent 1920x1072 -quality 82 -strip public/images/city-night-bg.jpg`
   - 모바일 세로: `-resize 900x1613^ -extent 900x1613 -quality 82` → `city-night-bg-mobile.jpg`
4. `understand_images`로 글리치(건물 뭉개짐 등) 검증 후 image-to-image 재수정.

## E. 베타/유료 스위치 (src/lib/betaConfig.ts)
- `BETA_FREE = true` → 오픈베타 무료모드(가격·결제 UI 숨김). 결제 코드는 **지우지 않고 숨김만** → 심사 통과 후 `false`로 한 번에 유료 복구.
- `OFFICIAL_PRICE = 29900` / `OFFICIAL_PRICE_LABEL = "29,900원"`.

## F. 페이지 구성 (src/app/)
- `page.tsx`(홈 히어로: hero-glass 카드+배지+h1+CTA "지금 무료로 진단 시작하기", BETA_FREE 조건부) / `diagnosis`(진단 폼, 국세청 조회 폴백 안내) / `community`(승인 사례+통계바+CTA) / `mypage`(메뉴판·매칭결과) / `admin`(회원/결제/진단서/매출/접속 탭) / `pricing` / `privacy`.
- 자동화: `vercel.json` cron `/api/crawl` 매일 20시(공고 크롤링).

## G. 검증·배포 순서(항상)
`npx tsc --noEmit -p tsconfig.json` → `npm run build`(300s) → (필요시 PM2 실서버 curl) → 토큰주입 git push main → Vercel 자동배포.

## H. 절대 규칙(재확인)
- **matching.ts 스코어링 로직 불가침** — 표시 후처리만.
- 팩트 근거·솔직·미래지향. 이해 안 되면 실행 전 재질문. 크레딧 절약.

---

## 주요 파일
- `src/lib/advancedScreening.ts` — INSTITUTION_LINKS(신보/기보/중진공/소진공/재단), JAEDAN_PRODUCTS, filterProducts(low 제거), InstitutionProduct 타입.
- `src/components/AdvancedScreeningPanel.tsx` — 결과리포트 렌더(기관 아코디언, approvalRank 정렬, 배지).
- `src/app/admin/page.tsx` — 관리자 대시보드(회원/결제/진단서/매출/접속 5탭).
- `src/lib/leadNotes.ts` — 리드 통화상태·메모(localStorage, DB 불필요).
- `src/app/page.tsx` / `privacy/page.tsx` — 진단·개인정보처리방침.

## 진행 이력(최근순)
- **(이번 세션) 💳🏅💎📢 결과 표시 개선 6종 (정책금융 상품 상위2개·소진공 승인율순 / 정부지원제도·감면 앞3개 펼침 / 그외지원 전부표시 / 문구 '다른')** [대표님 요청·승인("추천대로 다해줘"), 커밋 "ux: 💳 정책금융 상품 상위2개·소진공 승인율순 + 🏅💎 앞3개 펼침·문구 '다른' + 📢 전부표시"]
  - **원칙(불가침)**: matching.ts / computeSupportStatus / judge 판정 로직 0 수정. 전부 **표시 레이어(펼침 vs 접힘·정렬 후처리)만**. 값·판정·요약배너 카운트 불변.
  - **① 💳 정책금융상품 (AdvancedScreeningPanel.tsx)**: 기관마다 상품 상위 2개(`PRODUCTS_INITIAL_COUNT=2`)만 먼저 펼치고 나머지는 `showAllProducts[i]` "다른 상품 N개 더 보기 ▼"로 접음. `isTop`은 `products[topProductIdx] === prod` 참조비교(잘린 배열에서도 정확). 요약배너 `productsShownCount`는 전체 filtered 합산이라 접힘과 무관하게 전체 개수 유지.
  - **② 💳 정렬 = 혼합**: `approvalRank`(high0/mid1/low2) + `directFirstRank`(직접대출0). **`isDirectFirstInst`는 중진공만**(소진공 제외). 소진공은 상품마다 직접/대리 갈려 직접우선이 어색 → 승인율순만(대표님 "소진공도 승인율 높은 편 먼저"). 신보/기보/재단도 승인율만.
  - **③ 💳 대리대출(보증) 안내**: 신보/기보/재단 카드에 파란 info박스 "보증서 발급→은행 대출, 공단이 직접 주는 방식 아님".
  - **④ 🏅 정부지원제도 (AdvancedScreeningPanel.tsx)**: 기존 eligible만 펼침 → **앞 3개(`SUPPORT_INITIAL_COUNT=3`) 펼침 + 나머지 접기(접근 B)**. 제조업만 찍으면 eligible=혁신바우처 1개뿐 → 고용지원금·두루누리(potential)까지 끌어와 3개 채움. **배지는 정직하게**(eligible=신청가능 초록 / potential=조건 충족 시 가능 주황). 억지매칭 금지 원칙 유지. 문구 "조건 충족 시 가능한 **다른** 제도 N개 더 보기".
  - **⑤ 💎 감면혜택 (report/ExtraBenefitsSection.tsx)**: 동일 접근 B — `judged`(가능성순 상위6) 앞 3개(`BENEFIT_INITIAL_COUNT=3`) 펼침 + 나머지 "조건 충족 시 가능한 **다른** 혜택 N개 더 보기". 판정(judge)·정렬·개수 불변.
  - **⑥ 📢 그외 지원사업 (RelatedAnnouncements.tsx)**: 대표님 "그냥 다 보여줘" → `slice`·더보기·접기 완전 제거, `(items||[]).map` 전부 렌더. `showAllAnnouncements`/`INITIAL_ANNOUNCEMENT_COUNT` 삭제.
  - **QA(Playwright, 제조업 개인/3년미만/40세이상/직원0명)**: 🏅 혁신바우처(초록)+고용지원금(주황)+두루누리(주황) 3개 펼침·"다른 제도 2개 더 보기" ✅ / 💎 3개 펼침·"다른 혜택 N개 더 보기" ✅ / 📢 5개 전부·더보기 0 ✅ / 💳 대리대출 안내·상품 상위2개·소진공 승인율순 ✅. understand_images 배지색·순서 검증 완료.
  - **⚠️ 🌱 예비창업패키지 '3년차한테 뜸' 재현 시도 → 현재 코드에선 재현 안 됨**(개인/3년미만 프로필: 예비창업패키지는 '더 보기' 안으로 정확히 접힘, 펼침엔 초기창업패키지+원스톱만). isPreFounderEligible 로직 정상. 대표님이 본 화면은 1f2d118 배포 전이었을 가능성 → 이번 배포로 반영. 커밋 후 대표님 실서비스 재확인 요청.
- **(이번 세션) 🌱 창업지원사업 '해당되는 사람만' 먼저 표시(예창패/초창패/청년창업사관학교 등 자격 필터)** [대표님 요청·승인("응 그래도 가자 추천대로"), 커밋 "ux: 🌱 창업지원사업 자격자만 펼치고 비자격은 더보기로 접기"]
  - **배경**: 6개 창업지원사업(예비/초기/도약/재도전/청년사관학교/원스톱)이 모든 고객에게 고정 노출 → 대표님 "예창패/초창패/청년창업사관학교 이런것도 해당되는 사람만 뜨게해줘". 표시 방식은 옵션② (자격자 펼침 + 비자격 '더 보기' 접기 = 🏅💎와 동일 패턴) 승인.
  - **원칙(불가침)**: matching.ts 스코어링 0 수정. 표시 레이어 필터만. 목록·순서·내용·요약배너 카운트 불변.
  - **자격 판정(advancedScreening.ts `isPreFounderEligible(eligKey, profile)`)** — 진단 원본값 기준:
    - 예비창업패키지(`preliminary`) = 예비창업자(businessType "예비" or years "창업 예정")
    - 초기창업패키지(`early`) = 창업 3년 이내(예비/1년미만/3년미만)
    - 창업도약패키지(`leap`) = 창업 3년 초과~7년(5년미만/7년미만)
    - 재도전성공패키지(`restart`) = reFounder "예"(폐업 후 재창업)
    - 청년창업사관학교(`youth`) = age(만34/만39세 이하) AND 창업 3년 이내
    - 스타트업 원스톱센터(`always`) = 누구나(항상 표시)
  - **구현**: PreFounderProgram 타입에 `eligKey` 추가(6개 객체에 부여). AdvancedScreeningPanel.tsx 🌱 렌더를 `renderPreFounderCard` 헬퍼 + IIFE로 재구성 → 자격자 먼저 펼침 / 비자격은 "다른 창업지원사업 N개 더 보기 ▼"(주황)·"접기 ▲"(회색). 상태 `showPreFounderOthers`. 자격자 0명 극단 케이스엔 "지금 해당되는 창업단계 사업은 없어요" 안내 후 전체를 더보기로.
  - **검증**: Playwright 2케이스 — ①청년+예비: 펼침4(예비·초기·청년·원스톱)+"2개 더 보기" ✅ / ②5년차+40대: 펼침2(도약·원스톱)+"4개 더 보기" ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건).
- **(이번 세션) 결과창 아코디언 "접기 옵션B" — 신청 가능 먼저 펼침·조건 충족은 더보기로 접기(결정 마비 완화)** [대표님 요청·승인("응 그대로 가고"), 커밋 "ux: 결과창 아코디언 내용 접기(신청가능 먼저·조건충족은 더보기)"]
  - **배경**: 고객 피드백 "결과창 양이 너무 많아 뭘 해야 될지 모르겠다"(choice overload). 대표님 "아코디언은 유지, 안에 내용만 줄이자 → 접고 나중에 하라고 여지 남기기(고객 선택)". → **옵션B(의미 기준) 확정**.
  - **원칙(불가침)**: 판정/정렬/점수 로직 0 수정. 이미 계산된 결과를 "펼침 vs 접힘"으로 **표시만** 분리. 요약 배너 카운트·값 100% 보존.
  - **변경**:
    - 🏅 정부지원제도(AdvancedScreeningPanel.tsx): `status==="eligible"`(초록·신청 가능) = 펼침 / `"potential"`(주황·조건 충족 시) = "조건 충족 시 가능한 제도 N개 더 보기 ▼"로 접기. 상태 `showSupportPotential`, `renderSupportCard` IIFE.
    - 💎 감면(report/ExtraBenefitsSection.tsx): `v.status==="yes"`(초록) = 펼침 / `"condition"`(주황) = "조건 충족 시 가능한 혜택 N개 더 보기 ▼"로 접기. 상태 `showBenefitCondition`, `renderBenefitCard` IIFE.
    - 📢 그 외(RelatedAnnouncements.tsx): 상위 3개만 먼저 표시 + "다른 지원사업 N개 더 보기 ▼" / "접기 ▲". 상태 `showAllAnnouncements`, `INITIAL_ANNOUNCEMENT_COUNT=3`.
    - 💳 정책금융은 이미 "상품 보기" 토글 접힘 구조 → 손 안 댐.
  - **색상**: 더보기 버튼은 주황(brand-orange, 위험 아님), 접기 버튼은 회색. 레드는 위험 액션 전용(미사용).
  - **검증**: tsc+build EXIT 0. Playwright PC/모바일/💎펼침 스샷 3종 정상 확인, 대표님 승인. CopyGuard QA화이트리스트 제거(0건). matching.ts 0 수정.
- **(이번 세션) 5개 결과 아코디언 제목/부제 정리(제목=종류·부제=혜택, 반복 제거)** [대표님 요청, 커밋 "copy: 5개 아코디언 제목/부제 정리"]
  - **배경**: 결과 아코디언 5개의 제목↔부제가 서로 겹쳐 반복(특히 📢 "추가적인 그 외 정부지원사업 / 그 외 정부지원사업이에요"). 대표님 "제목과 아코디언 안 설명들과 어울리나? 깔끔하게 수정해볼까" → 제안표 승인("그래 다 그렇게 바꿔보자"). **표시 문구만 수정, 콘텐츠·매칭 로직 0 변경.**
  - **변경(제목=무엇인지 / 부제=대표님 이득)**:
    - 🌱 `예비·초기·청년창업자 지원사업` / `창업 단계 대표님을 위한 무상 사업화 자금이에요` (AdvancedScreeningPanel.tsx)
    - 🏅 `신청 가능한 정부지원제도`(유지) / `지금 바로 신청할 수 있는 제도만 모았어요` (AdvancedScreeningPanel.tsx)
    - 💳 `이용 가능한 정책금융상품`(유지) / `낮은 금리로 받을 수 있는 자금이에요` (유지)
    - 💎 `챙기면 좋은 감면 혜택`(제목 "추가" 제거, 모바일 2줄 유지) / `세금을 아낄 수 있는 혜택이에요` (report/ExtraBenefitsSection.tsx)
    - 📢 `그 외 놓치기 쉬운 지원사업` / `추가로 챙겨볼 만한 정부지원이에요` (RelatedAnnouncements.tsx)
  - **검증(Playwright PC 1280 + 모바일 390, ?admin=1, 아코디언 펼침)**: 5개 모두 제목/부제 정상 표시 시각 확인 ✅(PC·모바일 스샷 대표님 승인). 💎 모바일 2줄("챙기면 좋은"/"감면 혜택") 정상 ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건). matching.ts 0 수정.
- **(직전) 성능 최적화: 배경 이미지 WebP 변환(PC/모바일 렉 개선)** [대표님 요청, 커밋 REQ-30]
  - **진단**: next.config는 이미 잘 최적화됨(gzip compress, /_next/static immutable 캐시, public 자산 1일 캐시, HTML no-cache, optimizePackageImports). 폰트도 이미 최적(Pretendard 1종 + preconnect + media=print 비동기 로딩 + noscript 폴백). 번들도 양호(대부분 170~180KB). **최대 병목 = CSS 배경이미지가 원본 JPG 로드**.
  - **조치**: sharp(devDep)로 배경 2개 WebP 변환 — `city-night-bg.jpg` 355KB→**173KB(-51%)**, `city-night-bg-mobile.jpg` 130KB→**54KB(-58%)**. 배경은 어두운 그라디언트 오버레이(불투명 78~94%)가 덮여 q58로도 화질 저하 무시 가능.
  - **globals.css**: `background-image`를 3단 폴백으로 교체 — ①url(jpg) 기본폴백 → ②-webkit-image-set(webp/jpg) → ③image-set(webp/jpg). 최신 브라우저는 WebP, 미지원은 자동 JPG 폴백. 그라디언트 오버레이 유지.
  - **미사용 이미지**: approval-cases.jpg·naver-cover-*.jpg는 코드 미참조(WebP 안 만듦/삭제). next/image는 로고 2곳(Header/Footer)만 사용 — 이미 최적.
  - **matching-preview(215KB) dynamic import는 보류**: 구조 변경 최소 원칙 + 효과 대비 회귀 위험 커서 미적용.
  - **검증(Playwright PC 1280 + 모바일 390)**: 배경 `city-night-bg.webp 200` 실제 로드 확인 ✅. load PC 658ms / 모바일 373ms. 화면 정상(야경 배경·레이아웃·톤 무변경) 시각 확인 ✅. sharp는 devDependency라 Vercel 런타임 번들 무영향. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건). matching.ts 0 수정.
- **(직전) 🌱 아코디언 색 톤정리(구조 무변경)** [대표님 요청, 커밋 REQ-29]
  - **배경**: 5개 결과 아코디언 중 🌱 예비·초기·청년만 emerald(생 그린) 계열이라 톤이 튐. 나머지 4개(정부지원제도/정책금융/감면혜택/그외사업)는 이미 다크카드+brand-green 뱃지+brand-orange CTA로 통일돼 있음. **레이아웃·문구·버튼 위치/개수 0 변경, emerald 클래스 7곳만 brand 팔레트로 치환.**
  - **치환 내역(AdvancedScreeningPanel.tsx)**: ①상단 안내박스 `border-emerald-200 bg-emerald-50/60`+`text-emerald-800` → `border-brand-green/30 bg-brand-green/5`+`text-brand-dark/80`; ②사업 카드 `border-emerald-100` → `border-gray-200`; ③"사업화 자금" 뱃지 `bg-emerald-100 text-emerald-700` → `bg-brand-green text-white`(=신청가능 뱃지와 동일); ④금액 `text-emerald-700` → `text-brand-green`; ⑤🔗사이트 바로가기 `bg-emerald-600 hover:bg-emerald-700` → `bg-brand-orange hover:brightness-95`(=나머지 CTA와 동일); ⑥📄신청 매뉴얼 `border-emerald-600 text-emerald-700 hover:bg-emerald-50` → `border-brand-orange text-brand-orange hover:bg-brand-orange/5`.
  - **검증(390×844 Playwright, 만40세+법인, ?admin=1, 아코디언 펼침)**: 톤 통일 시각 확인 ✅(스크린샷 대표님 승인). emerald 잔여 0건. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건). matching.ts 0 수정.
- **(직전) 🌱 아코디언 REQ-28: 창업중심대학 삭제 + 공고시기 팩트반영 + 제목/문구 변경 + 모든고객 노출** [대표님 요청, 커밋 REQ-28]
  - **창업중심대학 항목 완전 삭제**: PRE_FOUNDER_PROGRAMS에서 해당 오브젝트 제거(7개→6개: 예비/초기/창업도약/재도전/청년창업사관학교/원스톱센터).
  - **공고시기 팩트체크 후 detail 반영**: web_search로 창업패키지가 매년 초(1~3월 상반기 1분기)에 집중됨 확인(예비 2월경, 초기·창업도약 1/23~2/13, 청년창업사관학교 1/30~2/13, 재도전 상반기). 5개 패키지 `detail`에 "1~3월 상반기", "매년 초", "상반기 집중" 등 공고시기 문구 추가. 원스톱센터는 상시라 제외.
  - **제목/subtitle 변경**: title "예비/초기/청년창업자 정부지원사업" → "**예비·초기·청년창업자 정부지원사업**"(가운뎃점 ·). subtitle → "**예비·초기·청년에 해당하는 대표님을 위한 정부지원사업이에요**".
  - **노출 조건 → 모든 고객**: 기존 `is_pre_founder OR (ceo_age<=39)` 가드 제거. AdvancedScreeningPanel.tsx `{( <AccordionCard.../> )}` 무조건 렌더. 청년/예비 체크 무관 **전원 노출**.
  - **상단 안내 박스**: REQ-26에서 이미 대표님 요청 문구와 일치("…정책자금(대출)과 달리 갚지 않아도 되는 지원금 중심이라…") → 변경 없음.
  - **검증(390×844 Playwright, 만40세이상+법인=비청년·비예비 프로필, ?admin=1, 아코디언 펼침)**: 비청년·비예비인데도 아코디언 노출 ✅, 제목/subtitle 일치 ✅, 창업중심대학 삭제 ✅, 공고시기(1~3월/상반기) 반영 ✅, 원스톱센터 유지 ✅. matching.ts 0 수정. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건).
- **(직전) 어드민 IP집계 봇의심 문구 삭제 + 원스톱 지원센터 추가** [대표님 요청, 커밋 REQ-27]
  - **어드민 문구 삭제**: admin/page.tsx IP집계 카드의 빨간 안내 `<p>`("🤖 봇 의심 뱃지 = …접속수가 많으면 'IP차단'을 고려하세요 (자동 차단 아님·표시만 됩니다)") 전체 제거(L2126~2130). 봇의심 뱃지 title 툴팁의 "접속수가 많으면 차단을 고려하세요" 문장도 제거(L2165). ※ 'IP차단' **버튼**·봇의심 뱃지 자체는 기능이라 유지, 상단 "접속수가 유난히 많거나…" 안내 1줄도 유지.
  - **🌱 아코디언에 스타트업 원스톱 지원센터 추가**(PRE_FOUNDER_PROGRAMS 7번째): 무료(정부 지원사업 통합 안내·상담 창구), 대상 "예비창업자 및 모든 창업기업", siteUrl `https://www.k-startup.go.kr/onestop` + manualUrl `/manuals/startup-onestop-manual-2026.pdf`(업로드 원본 복사, 200 OK). 사이트 바로가기 + 신청 매뉴얼 버튼 2종.
  - **검증(390×844 Playwright, ?admin=1, 아코디언 펼침)**: 원스톱 센터 노출·버튼 2종 시각 확인 ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건).
- **(직전) 🌱 예비/초기/청년창업자 정부지원사업 아코디언 개편** [커밋 55813fa]
  - **아코디언 제목 변경**: "예비창업자를 위한 정부지원사업" → "**예비/초기/청년창업자 정부지원사업**". subtitle도 "예비창업이거나 청년(만39세 이하)…"로.
  - **노출 조건 확장**: 기존 `is_pre_founder`만 → `is_pre_founder OR (ceo_age <= 39)`. 즉 예비창업 체크 or **청년(만39세 이하)** 대표님에게 노출. (profileToCompany: 만34세→30, 만39세→37, 만40세→45. 37·30 모두 ≤39 통과, 45 제외.)
  - **사업 6종으로 재구성**(PRE_FOUNDER_PROGRAMS, advancedScreening.ts L931~): 예비창업패키지·초기창업패키지·창업도약패키지·재도전성공패키지·청년창업사관학교(창업성공패키지)·창업중심대학. (기존 생애최초·신사업창업사관학교 제외, 대표님이 올린 6개 사업으로 교체.)
  - **버튼 2종화**: 타입 `applyUrl/applyLabel` → **`siteUrl`(① 사이트 바로가기) + `manualUrl?`(② 신청 매뉴얼 PDF)**. 각 항목에 🔗사이트 바로가기(초록 채움) + 📄신청 매뉴얼(초록 아웃라인) 버튼. 창업중심대학은 매뉴얼 미제공 → 사이트 바로가기만.
  - **사이트 URL**: 대표님 제공 K-Startup 공고 상세 링크 그대로(id=170001~170007, 청년창업사관학교 pbancSn=176107, 창업중심대학 id=171262).
  - **매뉴얼 PDF 5종** `public/manuals/`에 추가: startup-preliminary/early/leap/restart/youth-academy-manual-2026.pdf (업로드 원본 복사). 전부 200 OK·application/pdf 확인.
  - **⚠️ 중진공 청년창업자금(대출)은 '정책금융상품' 아코디언에 그대로 유지**(대표님 명시). 이 아코디언은 사업화자금(무상) 사업만.
  - **검증(390×844 Playwright, 청년+개인사업자 프로필, ?admin=1)**: 아코디언 제목 변경·6개 사업 노출·버튼 2종 전부 시각 확인 ✅. matching.ts 0 수정. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거(0건).
- **(직전) 예비창업자 버튼/사업자조회 문구 마무리 3종** [커밋 2574f43]
  1. **"⚠️ 사업자등록번호 10자리를 정확히 입력해 주세요" 문구 삭제**: `checkBno`·`confirmManualBno`에서 `digits.length!==10`일 때 `errorLength` 결과를 세팅하지 않고 `setBnoResult(null)`로 조용히 대기. (백엔드 route.ts L20 하드코딩은 별개, 프론트 표시 경로만 제거.)
  2. **예비창업자 버튼 = 조회 버튼 동일 스타일**: outline 토글식 → `btn-brand shrink-0 …`(오렌지 실선) 조회 버튼과 100% 동일 클래스.
  3. **예비 선택 안내를 하단 박스로**: 기존 plain `<p>` → 조회 결과 박스와 동일한 `rounded-xl border border-brand-orange/30 bg-brand-orange/10 px-4 py-3` 박스 안에 "**✅ 예비창업자를 선택하였습니다. 바로 다음 단계로 진행하세요.**" 표시.
  - **검증(390×844 Playwright)**: 10자리오류 안뜸·새문구 박스노출·옛문구 제거·예비버튼 btn-brand 4/4 PASS ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거 완료(0건).
- **(직전) 결격사유 박스 제목 간결화** [커밋 57aa8c2] diagnosis/page.tsx L861: "⚠️ 신청 결격사유 확인 (해당 시 신청이 불가)" → "**⚠️ 신청 결격사유 확인 (필수)**".
- **(직전) 예비창업자 바로가기 버튼 추가 + 안내문구 삭제** [커밋 965f99a]
  - 사업자 조회 박스 안내문구 `errorPreStartupHint`("아직 사업자 등록전이라면 …'예비'를 클릭하세요") **삭제** (config 정의도 제거).
  - **조회 버튼 오른쪽에 "예비창업자" 버튼 신설**(diagnosis/page.tsx L730 근처). 클릭 시 `set("businessType","예비")` → goNext의 `isPreStartup(startsWith("예비"))` 예외로 **사업자번호 없이 다음 단계 통과**. 기존 라디오와 완전 동일 값("예비")이라 매칭·검증 로직 100% 동일. 클릭 시 "✅ 예비창업자로 선택되었습니다…" 안내 + 아래 사업자구분 라디오도 '예비' 자동 활성(값 공유).
  - **검증(390×844 모바일 Playwright)**: 버튼 존재·문구삭제·국세청문구유지·클릭 후 안내·**사업자번호 없이 STEP2 통과·필수오류 미발생** 전부 true ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거 완료.
- **(직전) 결과창 실사 안내 문구 간결화** [커밋 965123a] AdvancedScreeningPanel L1402: "소액건은 비대면(모바일) 실사…규모가 큰 건은 방문 실사로 진행됩니다".
- **(직전) 진단 폼 문구/구성 5종 개선** [대표님 요청, matching.ts 불가침 준수]
  1. **신용점수 2구간→4구간**: `839점 이하·839점 이상` → **`745점 이하·839점 이하·839점 이상·점수 모름`**. ⚠️매칭 영향 검증 4/4 PASS: matching.ts L163-166이 "이하"→신용취약, "839점 이상"→신용양호, **"점수 모름"→어느 조건도 안 걸림=중립(신용취약 전용자금·교육·양호 일반자금 모두 미노출, 안전기본값)**. matching.ts 0 수정.
  2. **혁신성장 문구**: label "해당하나요?"→"**해당되나요?**", hint "기업이면 '예'"→"**기업은 '예'**".
  3. **purposes 순서 재배치**: 세무·노무·행정을 수출자금 다음으로 이동(창업지원·운전·시설·수출·**세무노무행정**·바우처·인증특허). hint "필요한"→"**필요하신** 항목을 모두 선택하세요". 값 불변 → 매칭 0 영향.
  4. **스마트기기 hint**: "서빙로봇"→"로봇", 끝에 "**등 자동화기기 쓰는 경우**" 추가.
  5. **재도전 hint**: "재창업한 소상공인" → "**재창업한지 7년이내 소상공인**".
  - **검증(390×844 모바일 Playwright, step2·step3 캡처)**: 신용점수 4구간·직원 6구간·purposes 순서·혁신 문구(step2), 스마트기기·재도전(step3) 전부 시각 확인 ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거 완료.
- **(직전) 직원 수 구간 세분화** [대표님 요청, matching.ts 불가침 준수]
  - `employees.opts`: `0명·5명 미만·5명 이상·50명 이상` → **`0명·5명 미만·5명 이상·10명 이상·30명 이상·50명 이상`** ("10명 이상"·"30명 이상" 2개 추가, 5~50명 사이가 넓어 세분화).
  - **매칭 영향 0 (검증 3/3 PASS)**: matching.ts L150 "소규모직원"(중진공 비제조 배제)은 0명/5명미만만 판정 → 10·30명 무영향. L339 신보유리 문구는 "10명이상"만 인식(코드 기존 존재, 옵션 없어 잠자던 것 → 이제 표시됨). **"30명 이상"은 어떤 조건에도 안 걸림 = 완전 무해**, 대표님이 "30명 신보유리 문구 안 떠도 됨" 확인 → matching.ts 0 수정(B안).
  - tsc+build EXIT 0.
- **(이번 세션) 진단 폼 문구/구성 3종 개선** [대표님 요청, matching.ts 불가침 준수]
  1. **현재 이용 정책기관에 '기타' 추가**: `currentInstitutions.opts`에 무보와 없음 사이에 "기타" 신설(소진공·중진공·재단·신보·기보·무보·**기타**·없음). ⚠️ matching.ts는 정식명(신용보증재단/기술보증기금/신용보증기금)만 중복배제(L400-402)에 사용 → "기타"는 `이용:기타` 태그만 생성될 뿐 어떤 매칭조건에도 안 쓰임 = **완전 무해**. matching.ts 0 수정.
  2. **맞춤 매칭 추가질문 4종 문구 정리**: 대괄호 태그 제거 — 대환(`갈아타고 [대환] 싶으신가요?`→`갈아타고 싶으신가요?`, hint "바꾸고 싶으신 경우"), 재도전(`다시 창업 [재도전] 중인가요?`→`다시 창업중인가요?`, hint "재창업한 소상공인"), 정부선정(hint "선정·지정"→"선정 및 지정"), 민간투자(`엔젤·VC`→`엔젤 VC`, hint "정부 지원 매칭을 원하는 해당되는 경우"). 안내문구 "안 누른 질문은 '아니요'로 인식합니다"→**"안 누른 질문은 자동으로 '아니요'로 체크됩니다"**.
  3. **전화 상담 문구**: `📞 전화 무료 상담을 원하시나요?`→**`📞 간단히 전화 무료 상담을 원하시나요?`** (hint 유지).
  - **검증(390×844 모바일 Playwright, step2·step3 도달)**: 기타 추가 ✅, [대환]/[재도전] 잔존 false, 4문구·전화라벨 전부 ✅, 가이드문구 ✅. tsc+build EXIT 0. CopyGuard QA화이트리스트(.sandbox.novita.ai) 제거 완료.
- **(이번 세션) 신청 결격사유 박스 톤 통일** [대표님 요청 — 커밋 2ad8102]
  - 결격사유 확인 박스의 빨간 제목 띠 제거 → 대표자 기본정보 박스와 동일한 표준 GroupBox(gray)로 통일. (위험=레드 규칙은 유지하되 이 박스는 정보성으로 판단해 통일)
- **(이번 세션) 홈 상하단 세로 여백 축소** [대표님 요청 — 커밋 4cc9695]
  - hero pt/pb + 하단 CTA pb를 한 단계씩 안쪽으로. (이전 중단된 시도가 git clean이라 미적용 상태였음 → 원값 기준 첫 실제 축소)
- **(이번 세션) 관리자 대시보드 '봇 의심 IP' 표시 기능** [대표님 요청 — 표시만, 자동차단 아님]
  - **배경**: 접속 로그에 텐센트클라우드(170.106.202.227, 미국·10회 접속·가입0)·DigitalOcean 같은 **데이터센터 IP가 접속수를 부풀림**. 실사용자는 대부분 KR ISP(KT·SKT·LGU+, 인천/경기). 대표님이 수동으로 판단해 차단하고 싶어 하심.
  - **구현**: 신규 `src/lib/ipClassify.ts` — 주요 클라우드/호스팅(Tencent·DigitalOcean·AWS·GCP·Azure·Oracle·OVH/Hetzner/Linode·Alibaba)의 **대표 CIDR 대역 내장** + 순수 `classifyIp(ip)` 함수. **외부 API 호출 0(완전 오프라인)**, 서비스 로직·고객 경로 영향 0.
  - **표시**: admin 페이지 'IP별 접속 집계' 테이블에 `🤖 봇 의심 · <사업자>` 뱃지 + 행 배경 로즈색. '최근 접속 로그'에도 🤖 소형 뱃지. 안내문구에 "데이터센터=봇 의심, 자동차단 아님·표시만" 설명 추가.
  - **안전성**: 어느 대역에도 안 걸리면 무조건 '일반' 판정(오탐 최소화). KR 통신사 IP는 목록에 없으므로 실사용자 오분류 구조적 불가. **차단은 기존 'IP차단' 버튼으로 대표님이 직접**(자동 차단 로직 없음).
  - **검증**: 로그의 실제 8개 IP 테스트 → 봇 2개(텐센트·DO)만 true, KR 6개 전부 false = **8 PASS / 0 FAIL**. tsc+build EXIT 0. PM2 재기동 후 /admin HTTP 200.
- **(이번 세션) 진단 폼 6종 개선** [대표님 요청, matching.ts 불가침 준수]
  1. **사업자 조회 오류 시 '직접 입력하고 계속하기' 시선 유도**: 버튼에 `animate-attentionPulse`(오렌지 링 확산+살짝 확대) + 상단 "👇 여기를 눌러 계속 진행하세요"(손가락 `animate-nudgeDown`) 추가. globals.css에 `@keyframes nudgeDown/attentionPulse` 신설.
  2. **연매출 6구간화**: `매출없음/1억미만/3억미만/5억미만/5억이상/10억이상`(기존 5개, 10억미만 제거·5억이상 신설로 6개). ※ matching.ts L63이 이미 "5억이상" 포함검사(하위호환) → "5억 이상" 선택 시 매출5억이상 태그→**신보 추천** 열림. matching.ts 무수정.
  3. **현재 이용 정책기관 목록 재구성**: 값(매칭검사 정식명)=소진공/중진공/신용보증재단/신용보증기금/기술보증기금/무역보험공사/없음, **화면 라벨만 labelMap으로 재단·신보·기보·무보** 축약(Multi 컴포넌트에 labelMap prop 추가). 무보(무역보험공사) 재추가. ⚠️매칭 중복배제(matching.ts L400-402가 정식명 정확일치 검사)는 값 보존으로 0 영향.
  4. **강점 그룹 순서 변경**: 특허·인증 → 혁신성장분야 순서를 **혁신성장분야 → 특허·인증**으로 교체(표시 순서만).
  5. **안내문구**: "해당되는 항목만 눌러 주세요. 안 눌러면 '아니요'로 체크됩니다." → **"해당되는 항목을 눌러주세요. 안 누른 질문은 '아니요'로 인식합니다."**
  6. **띄어쓰기**: "갈아타고[대환]"→"갈아타고 [대환]", "다시 창업[재도전]"→"다시 창업 [재도전]".
  - **검증(390×844 모바일 Playwright, 전체 플로우)**: 6항목 전부 ✅. 기관 긴이름 숨김·짧은라벨 노출 확인, 혁신성장(idx461)<특허(idx568) 순서 확인, 신보 추천 트리거(5억이상) 로직 검증. tsc+build EXIT 0. CopyGuard QA화이트리스트 제거 완료.
- **(이번 세션) 비회원 진단결과 게이트 → '맛보기 공개' A안 (전환율 개선)** [대표님 요청]
  - **요청**: "꽤 들어오는데 무료진단·회원가입을 안 한다. 결과를 조금 보여주고 회원가입하게 유도하자(2번 방식). 나중에 결제 붙이면 거기서 결제 유도되게 미리 세팅."
  - **팩트(조사)**: `matching-preview` 결과는 로그인 게이트로 통째 잠겨 있었음(비회원=guest면 🔒 화면만, 결과 0 노출) → 시크릿모드로도 회원가입 없이는 결과 못 봄. `BETA_FREE=true`라 로그인만 하면 결제 없이 전체 무료(previewLock/결제박스 꺼짐). 즉 유일한 벽=회원가입 1개인데, '가치'를 보기 전 가입 요구라 이탈.
  - **해법(A안, 표시 전용 — matching.ts 스코어링 불가침)**: `gate==="guest"` 화면을 재설계. ① 총 매칭 개수 크게 강조 ② 카테고리별 개수(정부지원제도/정책금융/추가감면) 그대로 공개 ③ `getMatchedTitles()` 실제 매칭 제목 **앞 3개만** `preview-lock-text`로 흐리게 노출 + "나머지 N개 더 있음" ④ '무료 회원가입' CTA. 개수/제목은 기존 `countMatchedItems`·`getMatchedTitles` 재사용(값 보존).
  - **미래 결제 대비 세팅**: `BETA_FREE` 분기로 CTA 문구·안내를 지금(무료 가입 유도) ↔ 나중(가입 후 결제 유도)이 스위치 하나로 자동 전환되게 심어둠. `betaConfig.BETA_FREE=false`만 바꾸면 맛보기→가입→결제 퍼널로 이어짐.
  - **검증(390×844 모바일, Playwright)**: 샘플 진단(제조·수출 개인사업자) → "총 30개 매칭 / 정부지원제도6·정책금융14·추가감면10 / 기관 3개 흐리게 / 나머지27개" 정상 렌더. tsc+build EXIT 0. 색상 규칙 준수(오렌지 강조, 위험 액션 아님→레드 미사용). 배지 "○○형" 없음.
- **(이번 세션) 모바일 배경(야경) 스크롤 시 흔들림 → 고정 (A안)** [대표님 요청]
  - **요청**: "모바일로 보면 뒷 배경이 스크롤할 때 바뀐다(밀린다). PC는 고정되던데 모바일도 고정 못 하나?"
  - **원인(팩트)**: 배경은 `body::before{position:fixed;inset:0}`로 뷰포트 고정이나, 모바일 브라우저는 스크롤 시 주소창 UI가 접혔다 펴지며 visual viewport 높이가 계속 변함 → fixed 배경이 그 변화를 따라가 스크롤 중 위아래로 밀려 보임. (PC는 주소창 변화 없어 완벽 고정) + PC뷰 강제(viewport 900 축소)와 겹쳐 더 도드라짐.
  - **해법(A안, 표시 전용 — 스코어링/데이터 무관)**: ① `layout.tsx`에 `BG_LOCK_SCRIPT` 추가(head, 뷰포트 스크립트 바로 뒤). 최초 로드 시 `window.innerHeight`와 `screen.height` 중 큰 값으로 `--app-vh`를 '못 박고', **스크롤 중 발생하는 주소창 높이 변화는 무시**(orientationchange 때만 재계산). ② `globals.css` `body::before`를 `inset:0` → `top/left/right:0 + height:var(--app-vh,100vh) + min-height:100vh`로 변경 → 배경 레이어 높이가 스크롤과 무관하게 고정.
  - **검증(390×844 모바일 렌더, Playwright)**: `--app-vh=1948px`. `body::before` 계산값이 스크롤 0px→`top:0,height:1948px`, 스크롤 1500px→`top:0,height:1948px`로 **완전 동일**. 실제 캡처로도 스크롤 전/후 야경 배경 위치·밝기 동일(흔들림·컷팅 0) 확인.
  - **한계 고지(대표님께 사전 안내함)**: 모바일 브라우저 주소창 특성상 100% 완벽 고정은 물리적으로 불가하나, A안으로 '거의 안 흔들리는' 수준까지 개선됨.
  - **파일**: `src/app/layout.tsx`(BG_LOCK_SCRIPT + head 삽입), `src/app/globals.css`(body::before 높이 고정). tsc+build EXIT 0. CopyGuard 임시 화이트리스트 검증용으로만 쓰고 제거 완료.
  - ⚠️**되돌리기 쉬움**: head의 `<script ...BG_LOCK_SCRIPT>` 삭제 + `body::before`를 `inset:0`으로 원복하면 즉시 복귀.
- **(이번 세션) 상단(헤더)·하단(푸터) 여백 안쪽 정리 + 전체 살짝 확대 (A안)** [대표님 요청]
  - **요청**: "우리 상단이랑 하단 조금만 안쪽으로 땡기고 전체적으로 살짝만 확대하면 더 보기 좋을 거 같다"(모바일 PC뷰 스샷 4장 제시, 헤더/푸터가 화면 좌우 끝에 딱 붙어 있고 콘텐츠가 좀 작게 눌려 보임).
  - **원인(팩트)**: 헤더 컨테이너 좌우 패딩 `pl-2 pr-2`(8px), 푸터 `px-4`(16px)로 얕아서 로고·메뉴가 화면 끝에 붙음. + PC뷰 강제(viewport 980) 때문에 콘텐츠가 다소 작게 축소돼 보임.
  - **해법(A안, 순수 표시 조정 — 스코어링/데이터 무관)**: ① 헤더 `pl-2 pr-2 sm:px-6` → (1차)`pl-4 pr-4 sm:px-8` → (2차 대표님 "조금만 더 안으로")`pl-6 pr-6 xs:pl-8 xs:pr-7 sm:px-12`. ② 푸터 `px-4` → (1차)`px-6 sm:px-8` → (2차)`px-8 sm:px-12`. ③ PC뷰 기준폭 `DESKTOP_WIDTH` 980 → **900**(전체 콘텐츠 약 9% 확대, 레이아웃 안 깨짐).
  - **검증(390×844 모바일 렌더)**: viewport 실측 `width=900, initial-scale=0.4333`(=390/900) 정확. 상단/하단 캡처로 헤더·푸터 좌우 여백 생김 + 전체 시원하게 확대 확인. 잘림 없음.
  - **파일**: `src/components/Header.tsx`(L49 패딩), `src/components/Footer.tsx`(L9 패딩), `src/app/layout.tsx`(DESKTOP_WIDTH 980→900 + 주석). tsc+build EXIT 0. CopyGuard 임시 화이트리스트 검증용으로만 쓰고 제거 완료.
  - ⚠️**되돌리기 쉬움**: 값만 원복(헤더/푸터 패딩, DESKTOP_WIDTH 900→980)하면 즉시 복귀.
- **(참고) 브라우저 간 아이콘 색 차이(삼성=오렌지/크롬=레드) 조사 → 코드 버그 아님(결론)**: Playwright로 크롬·삼성인터넷 UA 각각 실측한 랜딩 4카드 ✓아이콘 배경색 = 둘 다 `rgb(255,111,15)`=#FF6F0F 동일. 스샷의 색표현(삼성 디스플레이 채도/캡처 압축) 차이일 뿐 CSS 불일치 없음. 대표님 지시로 코드 변경 없이 그대로 둠.
- **(이번 세션) 모바일에서도 PC(데스크톱) 화면 강제 표시 - viewport 런타임 스크립트** [대표님 요청]
  - **요청**: "모바일 세로 레이아웃이 정보 많아 버겁다. 핸드폰으로 접속해도 '데스크톱 사이트' 켠 것처럼 PC 넓은 화면부터 보이게 해달라. PC/모바일 둘 다." (대표님이 실제로 삼성인터넷 '데스크톱 사이트' 체크 켠 화면을 좋다고 스샷 제시)
  - **팩트 고지**: 브라우저의 '데스크톱 사이트' 체크박스 자체는 사이트가 강제로 못 켬(사용자 브라우저 기능). 대신 viewport 폭 고정으로 '동일한 결과'(PC 화면 축소 표시) 구현 가능. 단 글씨 작아짐·통신사IP 등은 트레이드오프로 안내함.
  - **1차 시도 실패(교훈)**: `viewport.width=980` + initialScale 비움 → 브라우저가 자동 shrink-to-fit 안 하는 엔진에서 **화면 오른쪽 잘림**. 되돌림.
  - **최종 해법(성공)**: `layout.tsx` `<head>` 최상단에 런타임 스크립트 `DESKTOP_VIEWPORT_SCRIPT` 삽입(dangerouslySetInnerHTML, 첫 페인트 전 즉시 실행). 동작: `screen.width >= 980`(PC/태블릿)이면 표준 반응형 유지 / 그보다 좁은 모바일이면 viewport meta 를 `width=980, initial-scale=(기기폭/980)`로 교체 → 어떤 폰이든 980px PC 페이지를 화면폭에 딱 맞게 축소(잘림 0). orientationchange·resize 시 재계산. `viewport` export 는 `device-width` 표준값으로 되돌림(스크립트가 덮어씀).
  - **검증(390×844 모바일 렌더)**: 메인/진단 둘 다 PC 레이아웃 전체가 한눈에(메뉴 가로배치·카드 4열·진단 1단계 한 화면). initial-scale 0.398(=390/980) 정확 계산 확인. 잘림 없음.
  - **접근성**: `maximum-scale=5, user-scalable=yes` 유지 → 글씨 작게 느끼는 중장년 고객 핀치줌 가능.
  - **파일**: `src/app/layout.tsx`(viewport export + DESKTOP_VIEWPORT_SCRIPT + head script 삽입). tsc+build EXIT 0. CopyGuard 임시 화이트리스트 검증용으로만 쓰고 제거 완료.
  - ⚠️**되돌리기 쉬움**: 효과가 별로면 head의 `<script ...DESKTOP_VIEWPORT_SCRIPT>` 한 줄만 지우면 원래 반응형으로 복귀.
- **(이번 세션) 접속(IP) 로깅 전면 확장 - 회원가입/진단 안 한 '염탐꾼'까지 IP 다 잡히게 (A안)** [대표님 요청]
  - **문제(팩트)**: 기존 `logAccess`는 ①결과화면(/matching-preview) 한 곳에서만, ②그것도 '로그인한 사람'만 IP를 기록 → 회원가입도 진단도 안 하고 둘러보다 나가는 방문자는 관리자에 흔적 0. (홍길동처럼 가짜정보로 진단만 하고 이탈하는 애도, 순수 염탐꾼도 안 잡힘)
  - **대표님 요청**: "회원가입 안 해도 들어오는 모든 사람 IP 다 뜨게. 자주 오는데 진단·가입 안 하고 염탐만 하는 애들 조이게. 내가 어드민 보고 의심되는 IP 직접 차단할게." → **A안**(관리자에 다 뜨게 + 대표님이 직접 차단).
  - **수정1**: `deviceGuard.ts logAccess`에서 "로그인 세션 없으면 return" 조건 제거 → 비회원도 IP 기록. (IP는 `_cachedIp` 메모리 캐싱으로 페이지마다 ipify 재호출 방지)
  - **수정2**: `components/AccessLogger.tsx` 신설 → 루트 레이아웃(layout.tsx)에 심어 **모든 페이지**에서 경로 바뀔 때마다 `logAccess(pathname)` 실행. 관리자(/admin)·내부경로(_next/api/favicon) 제외, 같은 경로 연속 중복기록 방지.
  - **수정3(정리)**: `/matching-preview`의 개별 logAccess 호출 제거(AccessLogger가 커버 → 중복 제거).
  - **DB는 이미 준비됨(SQL 수정 없음)**: `access_logs.user_id`는 null 허용, RLS insert 정책이 `auth.uid()=user_id OR user_id IS NULL`이라 비회원 insert 허용, `log_access` RPC는 auth.uid()=null이면 email null로 저장. 관리자 '최근 접속 로그' 표는 이메일칸에 비회원 "-"로 표시(컬럼 그대로 재사용).
  - **관리자 UI 무변경**: IP별 집계·최근 접속 로그·차단목록·IP차단 버튼 전부 기존 그대로. 이제 비회원 로그가 채워지기만 함. 대표님이 자주 오는 IP 눈으로 보고 직접 '차단' 누르면 됨.
  - **파일**: `src/lib/deviceGuard.ts`, `src/components/AccessLogger.tsx`(신규), `src/app/layout.tsx`, `src/app/matching-preview/page.tsx`. tsc+build EXIT 0. CopyGuard 임시 화이트리스트 검증용으로만 쓰고 커밋 전 제거 완료.
  - ⚠️**IP 특정 한계(대표님께 고지함)**: KT 모바일 등 통신사 IP는 수천~수만명 공유 → 통째 차단 시 무관한 사람도 막힐 수 있음. IP는 ipify(클라이언트) 방식이라 우회 가능. '확실한 차단'이 더 필요하면 향후 서버 미들웨어(실 IP) 방식(B안)으로 승격 가능.
- **(이번 세션) 진단 3단계 톤 통일 + 결과창 예비창업 아코디언 상단이동/문구 + 모바일 줄바꿈 + 안내문구 수정** [대표님 4건 요청, 커밋 60e01ed→f99066e→209f834→(톤통일)]
  - **[톤 통일 - 질문지 1·2·3페이지 느낌 통일, 눈에 거슬리는 것 없이]** 대표님: "질문지 느낌이 1.2.3페이지 다 다른데 디자인이랑 질문지 느낌을 통일할수없을까 눈에 거슬리는거 없이" → **B안 확정**(옵션 UI는 유지: 1·2페이지 pill, 3페이지 카드 / 그룹박스 톤·색·여백만 통일).
    - `diagnosis/page.tsx`: 사업자조회 박스(빨강→회색)·대표자정보 GroupBox(tone="red"제거)·필요한지원 GroupBox(tone="orange"제거)·기업강점 GroupBox(tone="green"제거)·심층질문 박스(노랑→회색)·전화상담 박스(오렌지→회색) 전부 `border-gray-200 bg-gray-50/70` 1톤으로 통일.
    - **결격사유 박스만 빨강 유지**(위험=레드 규칙) → 세 페이지 통틀어 "여기만 위험"이라는 명확한 단일 신호.
    - **선택 강조=오렌지 1개로 통일**: pill 버튼 선택 + DeepCard active 모두 오렌지. bnoResult 상태박스(접수완료/서버점검)는 기능성 상태색이라 유지(범위 밖).
    - 다크테마 override(globals.css Rule1/2)가 회색 박스를 유리 톤으로 자동 처리 → 3단계 시각 QA 완료(눈 거슬림 없음 확인).
    - ⚠️**matching.ts·diagnosisConfig 값 미변경**(표시/색만). CopyGuard 임시 샌드박스 화이트리스트 검증용으로만 쓰고 커밋 전 제거 완료.
  - **[예비창업자 아코디언]** 결과창: 문구 "예비창업자를 위한 **창업 지원사업**"→"**정부지원사업**", 위치를 최상단(신청 가능한 정부지원제도 아코디언 위)으로 이동. 예비창업 체크자만 보이는 항목이므로 맨 위 노출이 자연스러움. `AdvancedScreeningPanel.tsx`. (`60e01ed`)
  - **[모바일 줄바꿈]** 결과창 "챙기면 좋은 추가 감면 혜택" → 모바일 2줄("챙기면 좋은 / 추가 감면 혜택"), PC 1줄. `AccordionCard` title 타입 string→ReactNode 확장 후 `<br className="sm:hidden"/>` 패턴. `AccordionCard.tsx`·`ExtraBenefitsSection.tsx`. (`f99066e`)
  - **[안내문구]** 진단 3단계 심층질문: "안 눌러도 '아니요'로 넘어갑니다."→"안 눌러면 '아니요'로 체크됩니다." `diagnosis/page.tsx`. (`209f834`)
- **(이번 세션) 진단 질문지 3단계 리디자인 - 흐름 재정렬·페이지 높이 균등화·심층질문 카드화** [대표님 요청]
  - **흐름 재정렬**: 1페이지 기본정보(사업자조회·대표자·**결격사유 유지**·사업장) → 2페이지 회사정보(자금여건·**필요한지원(purposes) 복귀**·강점) → 3페이지 맞춤 심층질문(카드 6종·전화상담).
  - **결격사유는 1페이지 유지(대표님 명시)**: "2페이지에서 이탈자 많아 처음부터 안 하는 게 서로 이득" → 문 앞에서 필터.
  - **심층질문 카드화(DeepCard)**: 6개(연매출성장📈·스마트기기🖥️·대환🔄·재도전🔁·정부선정🏆·민간투자💵) 탭형 체크리스트. 값은 예/아니요 그대로 저장 → **matching.ts 결과 100% 동일**. 미선택은 submit 시 "아니요" 자동. globals.css에 `.deep-card` 다크테마 규칙 추가(이모지·체크·카드 가시성 개선).
  - **페이지 세로 길이 균등화(대표님 요청)**: 측정 기준 step1 1604→1466 / step2 989→1267 / step3 1231→1029px. (purposes를 2페이지로 이동해 무게 이동, step1 업력·연매출·연령을 3열 배치해 압축, 심층카드 여백 확대.) 최대격차 615→437px로 크게 개선.
  - **값 불가침 준수**: diagnosisConfig.ts는 제목/부제/배치만 수정, opts 값 미변경. matching.ts·supportPrograms.ts 미변경.
  - CopyGuard 임시 샌드박스 화이트리스트는 검증용으로만 쓰고 커밋 전 제거 완료.
- **(이번 세션 후속) 배지 3종화 확정 + 문구/줄바꿈 업그레이드 + 소진공 직접대출 우선정렬 + 글자색·하이픈 정리** [대표님 A~D 요청 반영]
  - **상품 성격 배지 최종 3종 확정(직접대출/대리대출/보험)**: 위 2종 체계에 **보험(orange-100/700)** 추가. `LoanNature = "직접대출" | "대리대출" | "보험"`. 배지색 헬퍼 `natureBadgeCls`(직접=blue, 대리=purple, 보험=orange). 대리대출 = 보증부 = 보증서 = 확인서.
  - **[Request A] 팩트 vs 실무 충돌 2건 대표님 확정 반영**: ①**중진공 이차보전 = 직접대출로 표시**(4개 상품 청년전용창업·혁신창업사업화·신시장진출·신성장기반 전부 `nature:"직접대출"` 명시). ②**무보 단기수출보험(중소중견Plus+) = '보험' 배지**(`nature:"보험"`, 오렌지). (`0db12e9`)
  - **[Request B] 기관 안내·진행절차 문구/줄바꿈 전면 업그레이드**: 재단/중진공/소진공/무보 criteria에 `\n`·`\n\n`·【】 소제목 추가(`whitespace-pre-line` 렌더). 진행절차 박스 배지 라벨 정돈("대리대출(보증서→은행·재단/신보/기보/무보)", "직접대출(공단 직접실행·소진공/중진공)"), 소요기간 줄바꿈 분리. (`0db12e9`)
  - **[Request C] 소진공 결과리포트 '직접대출 먼저 추천' 정렬**: 소진공(소상공인시장진흥공단)만 2단계 정렬(직접대출 우선→승인가능성 순), 타 기관은 승인순 유지. 강조배지(topProductIdx)는 표시순서와 분리해 항상 최고 승인가능성 상품에 부착(정확도 보존). (`98f8f64`)
  - **[Request D-1] "※ 소액 건(재단)…" 글자 흰색 처리**: 다크테마 override 목록(globals.css)에 `text-brand-dark/55`가 빠져 어두운 배경 위에서 안 보이던 문제 → `.text-brand-dark\/55` 추가(→`rgba(244,245,247,0.75)`). 사이트 전역 /55 문자 동일 적용.
  - **[Request D-2] em-dash(—) → 짧은 하이픈(-) 전체 홈페이지 교체**: `src/` 전체 38개 파일의 `—`를 `-`로 일괄 치환(상품명 등 사용자 노출 문자열 포함). matching.ts의 6개는 전부 주석이라 로직 무관 확인 후 교체. tsc+build EXIT 0.
  - **파일**: `src/lib/advancedScreening.ts`, `src/components/AdvancedScreeningPanel.tsx`, `src/app/globals.css`. ⚠️matching.ts 스코어링 절대 미수정(표시 후처리만).
- **(이번 세션) 2026 공식 PDF 심층분석 → 결과리포트 정확도 대폭 향상 + 예비창업 아코디언 신설** [대표님 6문항 브리핑 답변 반영]
  - **상품 성격 배지 2종 체계로 정리(직접대출/대리대출)**: 기존 3종(직접대출/보증서/재단)에서 **직접대출(blue-100/700)·대리대출(purple-100/700) 2종**으로 통일. `LoanNature = "직접대출" | "대리대출"`, `InstitutionProduct.nature?: LoanNature | LoanNature[]`(상품 단위 지정, 없으면 `loanNatureOf(기관)` 폴백). 대리대출 = 보증부 = 보증서 = 확인서(현장 용어 통일). ※배지에 "○○형" 절대 금지.
  - **loanNatureOf 폴백**: 중진공=직접대출, 소진공=직접대출(기본), 신보·기보·무보·재단=대리대출.
  - **소진공(SMAN018M 공식표) 상품별 정확화**: 직접대출표 6종/대리대출표 7종 확정 반영. 일반경영안정자금=대리대출(기준금리+0.6%p), 혁신성장촉진자금 6개 variant=직접대출(기준금리+0.4%p), 재도전특별자금=직접대출(일반7천·희망1억·도약2억), 대환대출=대리대출(5천만·고정4.5%·최장10년), **민간투자연계형 매칭융자=직접대출 단독**(Q1 확정, 최대5억·시설포함10억).
  - **신보 3→5상품 세분화**: 일반보증(운전)·일반보증(시설)·퍼스트펭귄보증(최대30억, 업력<7)·스마트보증(최대2억·100%·0.7%)·수출기업우대보증(수출자). 전부 대리대출(보증서→은행).
  - **기보 3→4상품 세분화**: 청년창업기업보증(만17~39세·창업7년내·30억)·기술창업보증·기술보증(운전)·기술보증(시설). 전부 대리대출.
  - **무보 공식명칭화**: 수출신용보증(선적전)·수출신용보증(선적후)·단기수출보험(중소중견Plus+)·문화산업보증.
  - **🌱 예비창업자 전용 아코디언 신설(Q5)**: `report.company.is_pre_founder`=true일 때만 '사이트 바로가기' 바로 위에 노출. `PRE_FOUNDER_PROGRAMS`(예비창업패키지 최대1억·평균4천/생애최초청년 최대1억/청년창업사관학교 만39세·최대1억/신사업창업사관학교 최대4천) 사업화자금(무상) 중심, emerald 팔레트. autoRun 경로는 profileToCompany가 businessType"예비"→is_pre_founder 자동변환하므로 대시보드에서도 정상 노출.
  - **파일**: `src/lib/advancedScreening.ts`(LoanNature/loanNatureOf L825~, InstitutionProduct.nature L855, PRE_FOUNDER_PROGRAMS, 소진공/신보/기보/무보 상품 정확화), `src/components/AdvancedScreeningPanel.tsx`(배지 배열처리 렌더, 예비창업 아코디언). ⚠️matching.ts·runAdvancedScreening 스코어링 절대 미수정(표시 후처리만).
  - **⚠️ 팩트 vs 실무 충돌 목록(대표님 최종확인 필요)**: ①중진공 이차보전 — 대표님 Q2는 "대리대출 형태"라 하셨으나, 2026 공식 융자사업 안내는 거의 전 상품이 직접대출(이차보전은 예외적 공존). 현재 폴백=직접대출. ②무보 단기수출보험 — '보험'상품인데 폴백상 "대리대출" 배지가 붙음(엄밀히는 보증도 대출도 아님). 표기 방식 대표님 확정 필요.
- `daca8ff` **결과리포트 상품 성격 배지 추가**: 상품명 옆 작은 배지로 자금이 어떻게 나오는지 한눈에 안내. 기관 성격 기준 3종 — **직접대출**(소진공·중진공, 블루) / **보증서**(신보·기보·무보, 보라) / **재단**(지역신용보증재단, 그린). advancedScreening.ts에 `loanNatureOf(institution)` 헬퍼 추가(표시 후처리, matching 로직 무관), AdvancedScreeningPanel.tsx 상품명 span 뒤 배지 렌더. ※"○○형" 없이 직접대출/보증서/재단으로 통일(대표님 지정). ※미완료 5개 중 3번(괄호제거)=상품 구분정보라 미실행, 4번(ㅡ→-)=이미 코드에 긴대시 없어 스킵.
- (문서) **재현 플레이북 추가**: "아이템만 바뀌어도 이 수준 홈페이지 재현" 섹션 신설(A~H: 스택·브랜드색·다크테마·배경고정기법·이미지워크플로·베타스위치·페이지구성·배포순서·절대규칙). 지금까지 세션 노하우 총정리.
- `6af2366` **배경 완전 고정 + 살짝 어둡게**: 모바일 스크롤 시 배경이 콘텐츠 따라 밀려 바뀌던 현상 해결. body 직접 배경(attachment:fixed는 iOS 사파리 떨림버그)→**`body::before` 고정레이어**(position:fixed·inset:0·z-index:-1·pointer-events:none)로 분리. PC·모바일 모두 스크롤해도 배경 100% 고정. 오버레이 살짝 어둡게: PC 0.72~0.86→**0.78~0.90**, 모바일 0.74~0.90→**0.82~0.94**. (globals.css L19~46. body.theme-dark는 배경색 미지정이라 ::before 정상노출.)
- `25b1727` **홈 배경 야경 업그레이드**: 기존 도시야경(딥네이비#0B1325+골드불빛+수면반사) 정체성 유지하며 화질·디테일 업그레이드. nano-banana-pro로 3안(v1 수면반사스파클/v2 광원글로우Bloom/v3 지평선보라광운) 생성→실제 오버레이+히어로카드 얹은 스샷 비교→**v1 채택**(신뢰+활력·브랜드색조화·오버레이뚫는디테일). 상단 건물꼭대기 글리치 image-to-image로 수정. PC 1920×1072(363KB)·모바일 900×1613(134KB) JPG 최적화 교체. 파일: `public/images/city-night-bg.jpg`·`city-night-bg-mobile.jpg`. (globals.css 오버레이 rgba(8,12,24,0.72~0.86) 그대로.)
- `88168ff` 후기 CTA 문구 `내 사업장에 정부지원사업…`→`내 사업장에 **알맞은** 정부지원사업, 지금 무료로 진단받아 보세요.`
- `1643d96` 어드민 대시보드 안내 `회원·결제·조회권·매출을 한 곳에서 관리하세요.`→`관리하기`
- `822201b` 진단 국세청 점검 안내 문구 간결화(`지금은 국세청 조회 서버 점검 시간입니다.` + `직접 입력하고 계속하기를 클릭하시면 신청이 정상 접수됩니다.`). ※bnoServerDown일 때만 노출.
- `848b48a` 후기 문화산업보증 카드 상품명·기관명 축약(`문화산업보증`/`한국무역보험공사`) + 히어로 유입채널 `당근마켓·네이버블로그·카카오채널`→`당근 · 블로그 · 카카오 · 인스타 · 스레드`.
- `8fa88b9` 후기 소진공 카드: 지원금→정책자금, `일반경영안정자금(운전)`→`(대리)`, 배지 `지급완료`→`확인서 발급완료`(Approval에 `badge?` 옵션필드 추가, 미지정시 "지급완료" 기본).
- `b6c6f69` 후기 통계바 세로구분선 `divide-gray-200`→`divide-white/10`(연하게) + 후기 문화산업보증 카드 note `최대 승인 사례`→`신보/무보/중진공/소진공 총 9억 5천만원 승인 사례`.
- `848b48a`~ 참고: 후기(community) 페이지는 **다크 배경**(PageShell 다크). border-white/10 전면통일건은 무의미로 미실행, 테두리 gray-200 유지 + 통계바 divide만 white/10.
- `41dce17` **후기/마이페이지 미세 개선(다크테마)**: ①후기 상단 배지 `Customer Stories · 고객 성공 사례`→`이용자 정부지원사업 승인 사례`(community L244). ②후기 승인사례 데이터 `글로벌화 자금`→`신시장진출자금`(정책자금 1.5억원/중진공, community L169). ③마이페이지 하단 여백 축소 `py-6`→`pt-6 pb-4`(main L172). ④후기 페이지 하단 여백 축소 `pb-10`→`pb-6`(main L240). ⑤후기 CTA 문구 `내 사업장에 맞는 정책자금·지원금·바우처, 지금 무료로 진단받아 보세요.`→`내 사업장에 정부지원사업, 지금 무료로 진단받아 보세요.`(L397). ⑥어드민 진단서 요약박스(✅진단완료/⏳미완료) 두 카드 `text-center` 가운데정렬(admin L1566·1573). ※후기 페이지는 **다크 배경**(PageShell 다크). 대표님이 라이트로 오해했던 border-white/10 통일건은 실제 다크라 무의미 → 미실행(테두리 gray-200 유지). 통계바 세로줄 정리건은 대표님 답변 대기 중.
- `85888b9` 어드민 버튼 전체 hover scale-[1.02] 확대모션(상단·탭줄7·회원엑셀·회원액션6·진단서탭·기한연장).
- `efa37eb` 첫페이지 하단 CTA 카드~푸터 세로공백 축소(pb-8 sm:pb-12→pb-5 sm:pb-8).
- `22ba880` 진단링크복사→`https://모두의사업친구.kr` 첫페이지(/diagnosis 제거) + 어드민 진단서탭 완료/미완료 요약박스 흰·회색 통일 + 마이페이지 주요버튼 hover scale-[1.02].
- `0580a13` 후기(community) 테두리 brand-yellow→gray-200 통일(테두리·hover·구분선) + 마이페이지 만료문구 위 여백 mt-2.5→mt-5.
- `cc7980e` 탭명 `일주월 매출 리포트`→`요약 매출 리포트` + 어드민 버튼 흰/회색 통일(공고수집·새로고침·진단서엑셀·진단링크·회원엑셀·고객진단서·결과보기·기기초기화·조회권환불복구). **삭제·계정차단만 레드 유지**(대표님 "그대로 남겨"). 통계숫자·상태배지 유지.
- `9566ba7` 마이페이지 매칭결과 배너 테두리 `border-brand-red/10`→`border-gray-200`(배경 그라데이션 유지).
- `f44ff3c` **어드민 매출통계+요약리포트 병합** → 탭 1개 `📊 일주월 매출 리포트`(revenue 탭). 요약표(오늘/이번주/이번달/전체 × 신규가입·진단·결제·매출)를 매출탭 **맨 위**에 배치, 그 아래 기존 일별(30일)·월별(12개월) 상세표+드릴다운. **요약리포트 팝업 모달·버튼·state(showReport) 완전 제거**(기능 중복 제거). + **버튼 색상 팔레트 통일**: 대표님 지정 팔레트=**레드/그린(emerald)/오렌지/블루/반투명(white 5)/흰색**. 요약리포트 버튼(indigo) 삭제, 진단링크복사 white/gray→blue, 회원액션버튼 sky/indigo→blue·amber→orange·rose→red. (유입경로 배지 utmBadge는 채널 구분 목적이라 이모지+색 유지 — 추후 통일 여부 대표님 확인 대기.)
- `4ab2365` 진단링크 복사: 항상 한글 도메인 `https://모두의사업친구.kr/diagnosis` 하드코딩(copyDiagnosisLink). window.location.origin 퓨니코드(xn--) 방지.
- `566e0f1` 어드민 탭줄 순서·문구 변경(·→공백): 회원목록·고객진단서·결제조회권·매출통계·요약매출리포트·접속기기차단·진단서엑셀·진단링크복사 순. (※ f44ff3c에서 매출통계·요약매출리포트가 1개로 병합됨.)
- `43f85e8` 어드민 회원목록 기능추가(디자인 유지): ①표 헤더 클릭 정렬(가입일/최근접속/결제/누적금액, ▲▼) — state userSort{key,dir}, toggleUserSort, sortArrow. ②유입경로 필터 드롭다운(utmBadge 라벨+채널별 개수) — state userSourceFilter, userSourceCounts. 원본 검색(filteredUsers)·CSV 로직 그대로 두고 sortedUsers=filteredUsers→필터→정렬 후처리. 표 바디는 sortedUsers 사용.
- `037e870` 글자크기 통일: 탭줄 8버튼 13→14px, 전체N명 배지·회원표 헤더 12→13px.
- `2e9f94d` 탭줄 8버튼 flex-1 균등분배(빈공간 제거+구분선), 버튼명 '요약 리포트'→'요약 매출 리포트'.
- `171120c` 어드민 헤더 반투명박스, 하단 '관리자만 접근' 문구 제거.
- `f614ed6` 어드민 탭줄 빈자리에 **📊 요약 리포트 버튼**(모달). 오늘/이번주(월요일시작)/이번달/전체 × 신규가입·진단접수·결제·매출 표. state showReport, reportData=useMemo([users,diagnoses,payments]). 모달 위치=admin container 최상단(mx-auto max-w-6xl 직후).
- `c0e9a41` 어드민 회원검색창 폭 max-w-sm→max-w-md.
- `82359eb` 어드민 회원검색줄 정돈: 검색창 max-w 제한, 전체N명 반투명배지(bg-white/5 ring-white/10), flex-wrap+min-w-0로 가로 오버플로 방지. 회원표는 min-w-[920px]+overflow-x-auto라 세로로만 늘어남.
- `7734b26` 메뉴판: 정책자금 전화자문(10만원) 제거 → 8개(정부지원사업 러닝메이트 자문 포함).
- `800bc28` 메뉴판 문구정리(러닝메이트 자문 추가, 예비·초기창업패키지/세무조사·기장/수출 관세 상담/사업자 보험 상담).
- `802204f` **회원 CSV 전화번호 버그 수정**: downloadUsersCsv(admin L335)가 userInfoByEmail(이메일 정확매칭만) 써서 소셜로그인 이메일≠진단서 이메일이면 phone 못 찾음 → findUserDiagnosis(이름/연락처 폴백) 병용. + **메뉴판 글자 14px·1줄 고정**(mypage L300, text-[13px]→text-[14px]+truncate+whitespace-nowrap+min-w-0 flex-1).
- `d3ec934` 메뉴판 안내문구 2개 제거("필요하신 항목 눌러~" / "원하시는 서비스 버튼 누르면 카톡~").
- `87737cf` mypage 메뉴판 8개로 정리(POS·사업용자동차 제거). 어드민 탭줄 우측에 빠른실행 2버튼(🔗 진단링크 복사 → /diagnosis, 📋 진단서 엑셀). 메뉴판 위치=src/app/mypage/page.tsx L280~, 카톡링크 http://pf.kakao.com/_VxfWxan/chat.
- `46efef0` 어드민: '오늘 할 일' 요약 배너 제거(대표님 "안 씀" — 통화 미요청인데 카운트돼서 별로). 통화상태·메모·회원CSV·연결성은 유지.
- `0fb7439` 어드민: 리드관리(통화상태·메모)+오늘할일 요약+회원CSV+연결성(이름클릭→진단서) / 벽걸이 가로버전.
- `c193723` 신청 안내 3단계 문구 통일("담당 부처에 문의시 간편하게 신청 가능").
- `486d5a2` 결과리포트 low 상품 제거 + desk-summary(세로) 생성.
- `c362256`/`8fe7ace` 고객진단서 버튼 4단계 매칭 + 관리버튼 3×2 정렬.
- `9106b06` 개인정보처리방침 논리 정합(회원가입 시 이름·연락처 수집 명시).

## 결과리포트 정확도 개선 (advancedScreening.ts) — 처리결과
- [x] **상품 성격 태그**(2·5번 통합): `daca8ff`에서 완료. 직접대출/보증서/재단 3종 배지.
- [~] 1번(소진공/중진공 직접대출 우선정렬): 기관 간 정렬은 이미 step으로 처리됨. 기관 내 상품은 approval(승인율) 순 정렬이 우선이라 현행 유지.
- [–] 3번(상품명 괄호 제거): **미실행 확정** — 괄호 안이 상품 구분 핵심정보(일반형/혁신형 등)라 제거 시 구분 불가.
- [–] 4번(`ㅡ`→`-`): **스킵 확정** — 코드에 긴대시 없음(이미 `—`/`·` 사용).

## 산출물 URL
- 벽걸이 가로 PDF: https://www.genspark.ai/api/files/s/jhBNlbEy
- 벽걸이 세로 PDF: https://www.genspark.ai/api/files/s/NK8Ppece

## 셀프 업데이트/보안 진단 (현행 유지 결정) — 2026-07
- **결정: 현행 유지 (next@14.2.35 그대로).** 대표님 "응 현행 유지" 승인.
- npm audit → HIGH 2건이나, 우리 사이트 구조상 실제 공격 경로 없음:
  - SSRF(GHSA-p9j2-gv94-2wf4): rewrites 미사용 → **해당 없음**
  - Server Function 노출(GHSA-955p-x3mx-jcvp): Server Actions 거의 미사용 → 영향 극미
  - next/image 캐시 폭증(GHSA-3x4c-7xq6-9pq8): Vercel 서버리스 → 무관
  - postcss XSS/경로탐색: 빌드타임 도구, 런타임 영향 없음
- 유일한 fix 경로 = `npm audit fix --force` → **next@16.2.12 breaking change** (14→16 메이저 2단계). 위험 대비 실익 마이너스라 미적용.
- 14.2.x 라인은 이미 최신(14.2.35). 취약점 패치는 15/16 라인에만 존재.
- **향후 액션**: Next 15 안정화 시점에 계획적 마이그레이션(별도 세션, tsc+build 검증). 급하지 않음.

## 진단 1단계 예비창업자 버튼 색상 구분 (대표님 요청)
- 문제: 조회 버튼과 예비창업자 버튼 둘 다 `btn-brand`(노랑→오렌지)라 구분 안 됨.
- 처리: 예비창업자 버튼만 `bg-brand-green text-white`(#00C471)로 변경. className만 수정, onClick(set businessType "예비")·값·로직 100% 그대로.
- 위치: src/app/diagnosis/page.tsx 예비창업자 <button> (사업자번호 조회 옆).
- QA: 오렌지(조회) vs 그린(예비) 색 구분 확인, 클릭 시 안내박스+진행률5% 정상. "위험 액션만 레드" 원칙 유지.

## 진단 버튼 문구/색 + 아코디언 설명 정리 (대표님 요청)
- 진단 1단계 조회 버튼 문구: "조회" → "조회하기" (diagnosisConfig.ts BNO_TEXT.button).
- 예비창업자 버튼 색: 그린 → **화이트(border-brand-orange bg-white text-brand-orange)**. 조회하기(꽉찬 오렌지) vs 예비창업자(외곽선형) 주/보조 위계. onClick·값 그대로.
- 🏅 신청 가능한 정부지원제도 아코디언: 상단 "✅ 3스텝 미니 가이드" 박스 **삭제** → 다른 아코디언(💳/💎/📢)처럼 열면 바로 카드. (각 카드에 이미 📝 신청방법 있어 중복)
- 🌱 예비·초기·청년 아코디언: 무상 지원금 설명 **유지**(정보 가치 있음, 대표님 결정).
- QA: 조회하기/예비창업자 버튼 색 구분, 🏅 3스텝 삭제 확인, 🌱 설명 유지 확인 완료.

## 🌱 아코디언 설명 문구 변경 + detail 1줄 압축 (대표님 요청)
- 상단 설명(AdvancedScreeningPanel.tsx): 문구 교체 + <br>로 2줄 처리.
  "아래는 예비·초기·청년 창업자가 신청할 수 있는 대표적인 사업화 자금(무상) 지원사업이에요." (1줄)
  "정책자금(대출)과는 다르게 갚지 않아도 되는 지원금이며, 예비·초기 단계에서 챙기시길 권장드립니다." (2줄)
- PRE_FOUNDER_PROGRAMS 6개 detail(advancedScreening.ts) 모두 1줄로 압축. 공고시기 팩트(2월/1~2월/2~3월 등) 보존, 표현만 간결화. 중진공→약칭 등.
- QA: 2줄 설명 + 6개 카드 detail 각 1줄 확인 완료.
- ★결정 완료(대표님): 비회원 진단 완료 시 /signup 게이트(diagnosis/page.tsx L404~414) **유지**. "가입해야 결과 열람" 확정. 진단서(연락처)는 비회원도 DB 저장되므로 리드 확보 유지. 코드 변경 없음(이미 그렇게 동작).

## 진단서 라벨 "재창업[재도전] 여부" → "재창업 [재도전] 여부" (대표님 요청)
- diagnosisExport.ts L56 reFounder 라벨: "재창업[재도전]" → "재창업 [재도전]"(재창업 뒤 공백 추가).
- 이 라벨은 마이페이지 진단서 + 엑셀 export 공유. 한 곳 수정으로 전체 반영.

## 🌱 예비·재도전 완전 제외(방식 A) + 소진공 상품 3종 추가 + 졸업후보 기준 아코디언 (대표님 승인) — 2026-07
- **배경**: 대표님 버그 리포트 2건 — (1)예비 안 눌렀는데 예비창업패키지·재도전성공패키지 계속 노출, (2)소진공을 왠만하면 다 일반경영안정자금으로만 안내.
- **[방식 A] 🌱 창업지원사업 완전 제외** (AdvancedScreeningPanel.tsx):
  - `isPreFounderEligible(eligKey, 진단프로필)` 자격자(`eligibles`)만 렌더. 비자격 예비·재도전·청년·도약은 '더 보기'로도 안 보이게 **완전 제외**(others 변수·더보기 버튼·showPreFounderOthers state 삭제).
  - QA(unit): 개인·3년미만 → 초기창업+원스톱만 / 법인·7년이상 → 원스톱만. 예비·재도전 = 어느 프로필에도 노출 안 됨 확인.
  - 요약배너 카운트 영향 0 (PRE_FOUNDER는 onCounts 미포함).
- **[소진공 개편] INSTITUTION_LINKS(advancedScreening.ts) 상품 3종 추가** (matching.ts 불가침, eligibleWhen 조건 필터로만):
  - **소공인특화자금**: 제조/기술서비스(normalizeIndustry manufacturing|tech_innov)일 때, approval:high, 대리대출, 목록 최상단. 문구 "제조업(소공인)이라면 가장 수월하게 받는 편".
  - **신용취약소상공인자금(직접대출)**: nice/kcb ≤ 839, approval:mid, 직접대출. 839점 이하 전용 트랙 안내(신용관리교육 필수 명시).
  - **졸업후보기업자금(혁신형)**: 매출 ≥ 10억원(annual_revenue>=1,000,000,000)일 때만, approval:mid("조건 충족 시 가능"), 직접대출.
    - ★대표님 확정: 업종별 기준이 4.5억~42억 제각각 + 진단 매출 구간이 거칠어(최대 '10억 이상') 정밀 자동판정 불가 → 매출 최상단 구간만 "조건 충족 시 가능"으로 안내하고 정확 기준은 아코디언 표로 직접 확인. 영세 소상공인엔 미노출(억지매칭 방지). QA: 음식점 1억 프로필 → 졸업후보 안 뜸 확인.
- **[졸업후보 기준 작은 아코디언]** (대표님 요청 "작은 아코디언"=인라인 접이식, 팝업 아님):
  - 신규 `src/lib/graduationCriteria.ts`: GRADUATION_CRITERIA 61행(코드01~96, 업종/평균매출액/상시근로자, 6개 원본이미지 추출) + GRADUATION_EXCLUDED_NOTE(64~66·94·97~99 제외 각주).
  - AdvancedScreeningPanel: 졸업후보기업자금 상품 카드 아래에만(`prod.name.includes("졸업후보기업자금")`) 인라인 접이식 표. state openGradCriteria(키 `${i}-${pi}`). max-h-64 스크롤, sticky 헤더.
  - QA(Playwright 시각확인): 법인·제조·10억↑ → 졸업후보기업자금 + 아코디언 헤더 + 표(식료품 제조업/음식점 주점업 행) + 각주 정상 렌더 확인.
- **검증**: tsc 0 / build 0 / unit test(filterProducts+isPreFounderEligible 4프로필) 전부 정상 / Playwright 졸업후보 UI 시각 확인.
- **불가침 유지**: matching.ts / computeSupportStatus / judge 스코어링 미수정. CopyGuard clean 유지. 값 보존, 표시 후처리만.

## ⭐⭐⭐ [영구 규칙] SNS 홍보글 & 썸네일 제작 원칙 (대표님 반복 지시 — 절대 잊지 말 것) — 2026-07
> 대표님이 여러 번 지시했는데 계속 까먹어서 화나심. 이 3가지는 SNS/썸네일 작업할 때마다 **무조건** 먼저 확인.

1. **[썸네일 스타일 통일]** 항상 아래 "다크 네이비 + 골드 럭셔리" 스타일로 만든다. 파랑/초록 계열 절대 금지.
   - 배경: 깊은 남색(dark navy) 그라데이션.
   - 상단 중앙: 흰색 라운드 알약(pill) 배지 — 예 "소상공인정책자금".
   - 메인 타이틀: 골드 그라데이션 굵은 한글 2줄. (내용에 맞게, 과하게 채우지 말 것)
   - 서브 타이틀: 흰색 "2026년 신청 안내" 류 한 줄.
   - 얇은 골드 라이트 스트릭(빛줄기) 구분선.
   - 하단: 청라/도시 야경 스카이라인(어두운 톤).
   - 하단 중앙: 모두의사업친구 로고(주황 원 안에 소문자 m + 텍스트).
   - 전체: 얇은 흰색 라운드 테두리 프레임.
   - 참고 이미지(대표님 제공, 이 스타일 복제): 
     zHY5ka6l / uVH6EeFA / hb70eHX2 / B3tpxnqG / ZBt6tGxO / VtcuYTg4 / o07fhmgz / 2JvE1wDs / KtAiDDpc / 8GBaMYj0
   - 생성: image_generation, model=nano-banana-pro, 1:1, 2k, image_urls에 참고 이미지 넣어 스타일 복제.

2. **[글에서 · (가운뎃점/middot) 제거]** AI가 자주 쓰는 '·' 문자 절대 사용 금지. 대신 쉼표, 줄바꿈, 하이픈(-) 등으로 대체.

3. **[복사 붙여넣기 가능한 형식]** SNS 글은 반드시 복붙 가능하게. 마크다운 서식(**, ##, - 리스트 기호 등) 쓰지 말고 코드블록 안에 평문으로. 사용자가 그대로 긁어서 붙이면 되게.

- 채널별 분량: 인스타/스레드 = 제일 심도있게(힘주는 채널). 블로그/카카오채널/당근 = 간단히.

## ⭐ [영구 규칙 추가] SNS 채널별 노출 로직 & 링크 전략 (대표님 반복 지시) — 2026-07
- **5채널 동시 운영 중** → 같은 캠페인이라도 채널마다 노출 알고리즘이 달라서 글을 다르게 써야 함(똑같이 쓰면 도달 안 됨).
- **채널별 노출 로직(2026 기준):**
  - 인스타그램: 저장/공유/체류시간 우선. 첫 줄 후킹, 저장각 카드형, "다음 행동" 유도, 해시태그 5~10개, 진정성(AI 티 X).
  - 스레드: 자주 포스팅 + 대화(리플/재게시) 유발. 게시물 1개당 500자 제한 → 짧게. 첫 1시간 반응 중요. 이어달기(체인)로 깊이. 반말/직설, 질문으로 끝내기. **글 줄 때 본문/이어달기 몇 번째인지 명시해서 쭉 한 번에.**
  - 네이버 블로그: 검색 SEO. 제목/본문에 키워드(지역명+지원사업명) 반복, 소제목 구조, 정보량 많이.
  - 카카오채널: 구독자 대상 → 클릭률. 짧게, 혜택/마감 먼저, CTA 강하게.
  - 당근: 동네 이웃 톤. 광고티 X, "우리 동네 사장님" 정서, 자연 정보공유체.
- **링크 전략(대표님 확정):**
  - 인스타(프로필 링크) + 스레드(마지막 이어달기): https://link.inpock.co.kr/ppfa25 (인포크 = 클릭통계 추적)
  - 블로그 + 카카오채널 + 당근: https://모두의사업친구.kr (자사도메인 = 브랜드 신뢰 + SEO + 바로 전환)

## ✅ [완료] SNS 글쓰기 허브 (/admin/sns) — 2026-07 (방식 A 확정)
- **직원용 도구**: 관리자만 접근(admin_stats RPC 게이트, 미로그인/비관리자=🔒 denied).
- **경로**: `/admin/sns` (관리자 홈 상단에 "📣 SNS 글쓰기 허브" 버튼 링크).
- **방식 A(확정)**: AI/API 호출 없음. 폼 입력값을 검증된 템플릿에 끼워넣어 5채널 글 자동 생성 → 각 블록 "복사" 버튼으로 복붙.
- **엔진 파일**: `src/lib/snsTemplates.ts` (이 파일만 고치면 허브 글 품질 유지).
  - SnsInput(11필드), DEFAULT_INPUT(인천 소진공 융자 예시), stripMiddots(· 자동제거), parseHashtags(인스타 앞5개), buildInstagram/Threads/Blog/Kakao/Daangn, buildAllChannels, UPLOAD_GUIDES(채널별 올리는법), UPLOAD_ORDER(하루순서), THUMBNAIL_GUIDE(다크네이비+골드 규칙).
- **페이지 파일**: `src/app/admin/sns/page.tsx` (폼→라이브생성→탭→복사버튼→업로드설명서→썸네일규칙).
- **직원 사용법**: ① 사업 정보 폼 채우기 → ② 채널 탭에서 완성된 글 복사 → ③ 하루 업로드 순서/채널별 올리는법 참고 → ④ 썸네일은 규칙대로 제작.
- 커밋 499a178 (main 반영).

### 🔼 [업그레이드] 방식 A → AI 연결(방식 A 확정 후 AI로 전환) — 2026-07
- **결정 배경**: "링크만 주면 AI가 알아서 5채널 글 써주게" 요청 → 대표님이 OpenAI API 발급 + Vercel 환경변수 `OPENAI_API_KEY`(Production/Preview) + $10 크레딧 충전(자동충전 OFF).
- **엔진 파일**: `src/app/api/sns/generate/route.ts` (신규). POST `{source, region?, amount?, emphasis?}`. 링크면 서버가 본문 읽어와 요약(실패시 그대로 텍스트). `snsTemplates.ts`는 이제 **AI 실패 시 폴백**으로만 사용.
- **페이지**: `/admin/sns/page.tsx` = 링크/내용 붙여넣기 → "✨ 5채널 글 만들기" 버튼 → AI가 5채널 생성 → 탭+복사. 🟢AI생성 / 🟡기본템플릿 배지로 구분. (버튼 로딩상태 표시 예정)
- **모델**: `gpt-5.5` (env `OPENAI_SNS_MODEL`로 변경 가능). ⚠️ **GPT-5 계열은 커스텀 temperature 거부** → temperature 파라미터 넣지 말 것(400 방지). `response_format: json_object`는 지원됨. 대표님 계정엔 gpt-4o 없음(gpt-5.x만).
- **비용**: gpt-5.5 ≈ 150~300원/세트, gpt-5.4-mini ≈ 30원/세트.
- **프롬프트 = 대표님 실제 SNS 목소리 반영(A안, few-shot)**:
  - 대표님이 실제 운영 중인 스레드/블로그/카카오/당근 글을 예시로 프롬프트에 삽입 → 그 톤 그대로 이어서 작성.
  - **채널별 톤 분리**: 스레드=완전 반말 후킹체 / 블로그·카카오·인스타=“안녕하세요 대표님” 정중체 / 당근=동네 이웃 톤.
  - **고정 시그니처 훅**: "사업자번호만 넣으면 30초", "원래 39,900원 → 오픈 베타 무료", "행정대행 없음 수수료 0원, 대출 상담 아님".
  - **링크 방식(실제 운영 그대로)**: 블로그/카카오/당근 = `모두의사업친구.kr` 본문 직접 노출 / 인스타·스레드 = 주소 쓰지 말고 "프로필 하단 클릭" 유도. **인포크(inpock) 링크는 더 이상 안 씀**(과거 방식 폐기).
  - **개인 이력 금지 규칙**: 군 출신/컨설턴트/승인 건수/금액 실적 등 사람 이력을 글에 절대 넣지 않음(서비스·공고 가치로만 설득).
  - 실 채널 링크(참고): 인스타 instagram.com/ppfa25, 스레드 threads.com/@ppfa25, 당근 daangn.com/kr/local-profile/8j96yjujtkqy, 카카오 pf.kakao.com/_VxfWxan, 블로그 blog.naver.com/biospartners.
- **UI**: 썸네일 규칙 카드를 4번 업로드설명서 그리드(당근 옆)로 이동 + 배경 흰색 통일(눈 편하게). 커밋 4f50560.

### 🔧 [개선] SNS 허브 UX — 상태 유지 + 버튼/가이드 다듬기 — 2026-07
- **상태 유지(핵심)**: 직원이 글 생성 후 다른 채널에 붙여넣으러 뒤로가기 → 다시 허브 진입 시 **기존 생성글이 날아가지 않고 그대로 유지**. `sessionStorage("sns-hub-draft")`에 channels/mode/note/activeTab/source/region/amount/emphasis 저장 → mount 시 복원. **덮어쓰기는 "다음 글 만들기"를 새로 성공했을 때만**. (탭 완전히 닫으면 초기화 — 필요시 localStorage로 영구화 가능.) 커밋 2eef3d9.
- **버튼 3단계**: "✨ 5채널 글 만들기 → (스피너)✨ 작성중 → ✨ 글 완성"(2초 후 원복). 뒤 반투명 박스는 글자 크기에 딱 맞게 축소 + 글자 정중앙. 커밋 ceb5a12/36a4b4c/2eef3d9.
- **가이드 간결화**: 채널별 올리는법 3~4단계로 압축, 하루순서 부연 제거, 썸네일 규칙 4줄. 인스타 "링크는 프로필 하단 참고", 당근 "당근 소식" 탭/"지역 확인 후 게시", 썸네일 "도시 야경". 커밋 3d219c7/36a4b4c.
- **관리자 버튼 화이트 통일**: 상단 3버튼(SNS/공고수집/새로고침) + "← 관리자 홈" 흰색 테두리 통일, 안내문구(가운뎃점/노출로직) 삭제. 커밋 7003f92/3cfab69.

### 🖼️ [진행] 썸네일 워크플로우 & 객체 분리 이미지 — 2026-07
- **워크플로우 확정**: 허브에서 썸네일 자동생성 X. 직원이 허브에서 글 생성 → 대표님께 전달 → **대표님이 썸네일 직접 제작**. 캔바에서 조립하는 방식.
- **객체 분리 세트(제작 중)**: (a) 빈 배경 템플릿(다크네이비+골드+도시야경) (b) 하단 로고 투명 PNG (c) 상단 배지(흰 동그라미) 투명 PNG (d) 글꼴/글자크기 스펙. 직원이 캔바에서 배경 위에 로고/배지 올리고 골드 타이틀 2줄 얹어 조립.
- **썸네일 규칙(고정)**: 다크네이비+골드 럭셔리 톤(파랑/초록 금지), 골드 타이틀 2줄+흰 서브타이틀, 하단 도시야경+로고, 5채널 동일 썸네일 1장 통일.

### 🎨 [확정] 썸네일 글꼴/글자크기 스펙 (직원 전달용, 캔바 기준) — 2026-07
- ⚠️ 참고: 완성본 10장은 AI 생성이라 "원본 폰트명" 단정 불가. 아래는 완성본 모양을 재현할 수 있는 **무료 상업용 한글 폰트 + 크기**로 정리한 값.
- **기본 규격**: 1080×1080 정사각(인스타·당근·블로그 공용), 다크네이비+골드(파랑/초록 금지), 흰 라운드 테두리, 5채널 동일 1장.
- **글꼴**:
  - 메인 타이틀(골드 큰 글자) = **검은고딕(Black Han Sans)** ← 완성본 굵고 꽉 찬 느낌에 가장 근접. (대체: G마켓 산스 Bold)
  - 상단 배지(흰 캡슐 속) = **프리텐다드 ExtraBold** (대체: 노토산스 KR Bold)
  - 서브타이틀("2026년 신청 안내") = **프리텐다드 Medium** (대체: 노토산스 KR Regular)
  - 하단 로고 글자 = 로고 이미지 그대로 사용(타이핑 X)
- **글자 크기(1080×1080)**: 상단 배지 40~46pt / 메인 타이틀 2줄 100~120pt(1줄 짧으면 130~150pt) / 서브 44~52pt / 하단 로고 가로 300~340px.
- **색상 코드**: 배경 #0A1628~#16233F(그라데이션) / 골드 #E6C36A / 흰색 #FFFFFF / 배지 검정 #111111.
- **조립 순서**: 배경+하단야경 → 상단 흰캡슐+카테고리 → 중앙 골드 타이틀 2줄 → 골드 가로 광선줄 → 흰 서브 → 하단 로고.
- **완성 자산(이번 세션)**: 빈 배경 템플릿(로고 포함, 중앙 글자만 빈) = https://www.genspark.ai/api/files/s/tvkrCwTY / 상단 배지 세그먼트 = https://www.genspark.ai/api/files/s/57vYW3By. (하단 로고 투명 PNG는 대표님 지시로 제작 보류.)

### 🚀 [대개선] SNS AI 프롬프트 전면 재설계 — 채널별 2025~2026 알고리즘 반영 — 2026-07
> 대표님 지시: "글이 허접하고, 줄바꿈 안 되고, 사진 넣을 구간표시 없고, 채널 로직 미반영. 각 SNS 알고리즘에 맞춰 노출 잘되게, 전문가급으로, 잘 터진 컨텐츠 참고, 직원은 적은 작업만 하게 최대 자동화." → `/api/sns/generate` 시스템 프롬프트 전면 재작성.
- **페르소나**: "대한민국 상위 1% SNS 마케터가 모두의사업친구 공식 채널을 직접 운영" 톤. 광고카피가 아니라 계정을 오래 운영한 사람이 다음 글 쓰듯.
- **최우선 규칙(직원이 손 안 대게)**:
  1. **줄바꿈 강제** — 벽글 금지, 1~2문장마다 `\n`, 단락 사이 `\n\n`. (UI는 `<pre whitespace-pre-wrap>`로 실제 줄바꿈 렌더 → 그대로 복붙 가능.)
  2. **사진 구간 표시** — 이미지 자리는 본문 안에 `[📷 사진① 표지 - "..." 골드 타이틀 썸네일]` 형식으로 어떤 사진인지 구체 지시.
  3. **복붙 즉시** — 마크다운(**, ##, - ) 금지, 가운뎃점(·) 금지(쉼표 대체).
  4. **팩트 고정** — 공고에 없는 수치(금리/한도/마감) 지어내기 금지, 개인이력 금지, "100% 승인/대행" 금지.
- **채널별 알고리즘 맞춤(web_search 심층분석 반영, 채널마다 완전히 다른 글)**:
  - **인스타(캐러셀)**: 저장수+DM공유가 도달 결정. 카드 8장(표지 후킹→정보 6장→CTA) 각 사진구간 표시 + 캡션(저장/공유 유도) + 해시태그 10개(#정부지원사업 #모사친 필수).
  - **스레드**: 첫 3초 체류+첫1시간 댓글이 도달 폭발(리포스트1=좋아요10). 완전 반말, `[1/5]` 이어달기 각 500자 이내, 댓글 유도로 마무리, 링크 주소 노출 금지("프로필 하단").
  - **네이버 블로그**: C-Rank(출처신뢰)+DIA(깊이/체류), 이미지 없으면 저품질. 제목 SEO(지역+사업명+연도), 본문 7~9단락 + 사진구간 3~5개, 자사도메인 본문 삽입, 태그 8~10.
  - **카카오**: 구독자 대상 짧게, 후킹 한 줄+혜택 압축+CTA 하나, 자사도메인 직접 노출.
  - **당근**: "홍보 아닌 척" 간파당함 → 업체명 반복 금지, 지역정보로 접근, 이웃에게 정보 공유하듯, 자사도메인 1회.
- **JSON 스키마 변경**: 인스타 blocks = `ig-cards`(사진구간 포함) / `ig-caption` / `ig-hashtags` (**구 `ig-link` 블록 제거** — page.tsx가 blocks를 동적 map하므로 UI 안전). 스레드 `th-chain`, 블로그 `blog-title`/`blog-body`/`blog-tags`, 카카오 `kakao`, 당근 `daangn`.
- **링크 규칙**: 블로그/카카오/당근 = 본문에 `https://모두의사업친구.kr` 직접 노출 / 인스타·스레드 = "프로필 하단 클릭"(인포크 폐기, DEFAULT_LINK_INPOCK = HOME으로 하위호환).
- **검증**: tsc OK, build OK. route.ts 262→295줄. 커밋 예정.

## ⭐⭐⭐⭐ [최우선 고정 규칙] 썸네일 제작 방식 — 대표님 확정 (다시 새로 그리지 말 것) — 2026-07
> 대표님이 여러 번 화내며 지시함. **썸네일 요청 오면 아래 방식 그대로만.** 새 스타일 그리기 절대 금지, 확인 질문 없이 바로 이 방식으로 뽑는다.

### ✅ 확정된 유일한 제작 방식
1. **완성본 레퍼런스를 image_urls로 넣고 nano-banana-pro로 편집(edit)** 한다. 텍스트만 교체, 나머지는 그대로 유지.
   - 표지/일반 카드 레퍼런스: `https://www.genspark.ai/api/files/s/zHY5ka6l`
   - CTA(마지막) 카드 레퍼런스(로고+.kr+별점 톤): `https://www.genspark.ai/api/files/s/8GBaMYj0`
2. **고정 프롬프트 뼈대**(영어로, 이 문장 그대로 재사용):
   > "Edit this Korean thumbnail. Keep EVERYTHING identical: dark navy background, Chicago night city skyline at bottom, thin white rounded border, white capsule badge at top, 3D glossy gold beveled Korean title font, golden horizontal glow line, white subtitle, bottom orange 'm' circle logo with '모두의사업친구'. ONLY change text: 배지 / 골드 타이틀 2줄 / 흰 서브. Korean must be perfectly spelled."
3. 모델 `nano-banana-pro`, `aspect_ratio: auto`(레퍼런스 정사각 유지), `image_size: 2k`.
4. **생성 후 반드시 understand_images로 한글 오타 검수**(한글 이미지는 오타 잘 남). watermark 없는 URL(`image_urls_nowatermark`)로 검수.
5. 인스타 캐러셀이면 **카드1(표지) 1장으로 톤 확정 → 나머지 카드 병렬 생성**. 각 카드 = 배지(카테고리) + 골드 타이틀 2줄 + 흰 서브 1줄.

### 🎨 고정 디자인 스펙 (절대 불변)
- 배경: 다크네이비(#0A1628~#16233F) + 하단 시카고 야경 실루엣. **파랑/초록 절대 금지.**
- 상단: 흰 캡슐 배지(검정 글씨) = 카테고리명.
- 중앙: 골드(#E6C36A) **3D 입체(beveled/glossy) 굵은** 한글 타이틀 2줄 + 그 아래 골드 가로 광선줄.
- 서브: 흰색, 골드줄 아래 한 줄(예: "2026년 신청 안내").
- 하단: 주황 원형 m 로고 + "모두의사업친구" (CTA 카드는 그 아래 "모두의사업친구.kr").
- 규격 1080×1080 정사각. 5채널 동일 톤.

### 🚫 금지
- 개인 이력(군출신/컨설턴트/승인건수/금액) 문구 금지. 가운뎃점(·) 금지. "100% 승인/무조건/대행" 금지.
- 공고에 없는 수치(한도/금리/마감) 지어내기 금지 → "공고문에서 확인"으로.
- **새 배경/새 스타일 창작 금지.** 반드시 위 레퍼런스 edit 방식.

### 📌 검증된 완성 사례 (제물포구 기업환경 개선사업 8장, 2026-07 — 이대로가 정답)
표지 3ihJEpbu / 지원대상 ljL0HRkm / 근로자기준 N5Sv6b6X / 사업개시 8Qn4dsGZ / 지원내용 vX9wnuTy / 지원한도 apFtoJhR / 신청방법 dZItBXFH / CTA uRwVrXS1 (모두 https://www.genspark.ai/api/files/s/ 접두)
